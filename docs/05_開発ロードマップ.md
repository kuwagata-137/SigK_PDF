# SigK PDF 開発ロードマップ

作成日: 2026-08-29（同日改訂。`docs/07_開発計画の決定事項.md` を反映）
関連: `docs/01_製品要件定義.md`（機能ID）／`docs/02_アーキテクチャ設計.md`（モジュール）

各フェーズは「終わったら動くものが手元に残る」単位で切る。フェーズの終わりにはインストーラーを1本作り、実機で触れる状態にする。着手前に仕様を確定させる進め方は `.claude/CLAUDE.md` 領域1に従う。

---

## Phase 0 — 土台

**目的**: 空のウィンドウが出て、テストと CI が回り、インストーラーが作れる状態にする。

| # | 作業 | 完了の判定 |
|---|---|---|
| 0-1 | `package.json`・`main.js`・`preload.js`・`index.html` の骨組み | `npm start` でウィンドウが出る |
| 0-2 | `scripts/vendor.js`（pdf.js・pdf-lib・fontkit を `vendor/` へ複製） | `npm install` 後に `vendor/` が揃う |
| 0-3 | `app://` スキームの登録と、セキュリティ設定（`sandbox`・CSP・外部通信の遮断・新規ウィンドウの拒否） | 設定値の確認テストが通る |
| 0-4 | デザイントークンの `<style>`（ライトのみ）と画面の骨組み | モックの**画面の枠組み**（タブバー・ツールバー・ツールレール・サイドパネル・ステータスバー）と配色トークンが `index.html` と `renderer/shell.css` に移り、`npm start` の描画がモックと一致する。**モック内の疑似的なページ内容とサムネイルの中身は移さない**（Phase 1 で pdf.js の実描画に置き換わるため） |
| 0-5 | テストハーネスの新規作成（CheckListMaker は参照できないため移植ではない。`docs/07` 第1章 #5 の訂正） | `npm test` が緑 |
| 0-6 | CI 2本（`test.yml`・`build-windows.yml`） | Actions でインストーラーが Artifact に出る |
| 0-7 | `THIRD-PARTY-NOTICES.md` の初版 | 依存4件のライセンス全文が入っている |

**論点1 は着手前に決着済み**: `docs/02_アーキテクチャ設計.md` 第9章の論点1（pdf.js 4系の ES Modules 配布が `file://` で読めるか）は、Chromium での実測により「読めない」と判明した。3系へ落とすのではなく、レンダラーの配信を `app://` へ変えることで解決する（`docs/07_開発計画の決定事項.md`）。Phase 0 では `app://` の土台までを作り、pdf.js の読み込みは行わない。

---

## Phase 1 — 閲覧とページ操作

**目的**: PDF を開いて読める。ページを並べ替えて保存できる。**このフェーズの完了時点から常用を始める**（`docs/07_開発計画の決定事項.md` 決定5）。

常用を始める以上、動作確認用のインストーラーとは要求水準が違う。実ファイルを毎日通すため、後段から2件を前倒しした（決定6）。

| 前倒し | 元の位置 | 理由 |
|---|---|---|
| 上書き保存時の `.bak` 作成 | 6-2 | 保存は `ops` をワーカーが元ファイルへ適用する経路であり、バグは書き損じではなくファイルの破壊として出る。常用初期は `ops` の種類が増えていく最中で、最もバグが出やすい |
| コマンドライン引数の解釈＋単一インスタンス | 5-1・5-2 | これが無いとエクスプローラーからダブルクリックで開けず、毎回アプリを起動してからファイルを開くことになる |

`PENDING_WINDOW_MS`（`docs/03_Windowsシェル統合設計.md` 3-2）の実測は**前倒ししない**。引数の集約が要るのは、右クリックメニューが N 個のファイルに対し N 個のプロセスを起動する `merge`・`split`・`toPdf` の場合であり、いずれも Phase 2 以降である。Phase 1 の `open` は、パスが届くたびにタブを1枚足せば済む。実測は Phase 5 に残す。

### 作業の刻み

15項目を PR に分ける（決定7）。①を先頭に置くのは、`app://` と pdf.js の統合が実際に動くかを最初に確かめ、駄目なら配信方式の判断を早期に見直せるようにするためである。

| 塊 | 内容 | 対応要件 | 仕様書 |
|---|---|---|---|
| ① 表示の最小 | `app://` からの配信、pdf.js の統合、連続スクロール、ズーム、ページ移動、ファイルを開く（ダイアログのみ・1文書） | F-01-1（連続スクロール）、F-01-2、F-01-3、F-06-1（ダイアログ経路） | `spec-1-1-viewer.md` |
| ② 入出力とタブ | ファイルを開く（ドラッグ＆ドロップ）、タブによる複数文書、最近使ったファイル、文書情報 | F-06-1（残り）、F-06-3、F-06-4、F-01-7 | `spec-1-2-tabs.md` |
| ③-a サムネイルとテキスト | サムネイルのサイドパネル、テキストレイヤー、テキスト選択・コピー | F-01-4、F-01-6 | `spec-1-3-reading.md` |
| ③-b 検索と印刷 | テキスト検索（ハイライト・前後移動）、印刷 | F-01-5、F-01-8 | `spec-1-4-find-print.md` |
| ④ ページ編集 | ページモード（並べ替え・回転・削除・複数選択）、ページの並び（`pages`）の組み立て、undo/redo、未保存の表示と確認 | F-02-1〜F-02-3、F-02-6、F-02-7 | `spec-1-5-page-edit.md` |
| ⑤ 保存の完成 | ワーカー（`utilityProcess`）と進捗・中止、保存・名前を付けて保存・未保存の警告、**抽出・挿入**、`.bak` の作成、パスワード付き PDF の読み込み、コマンドライン引数＋単一インスタンス | F-02-4、F-02-5、F-06-2、F-06-5、F-06-6、F-07-1、非機能 | — |

**2026-08-31 改訂: 塊③ を ③-a と ③-b の2本に割った**（PR は5本 → 6本）。理由は2つ。

- 量が塊② を超える。塊② はレンダラー5本追加・テスト77件増だった。サムネイル・テキストレイヤー・
  検索・印刷の4本立ては明らかにそれより大きく、決定7 の「終われば何かが新しくできる」単位として
  大きすぎる。
- **依存の向きが片方向である。**テキストレイヤー（選択・コピー）は検索ハイライトの土台になる。
  先に作れば後半が素直に載る。逆は成り立たない。印刷も、ページを描く経路が固まってからのほうが
  作りやすい。

**進捗**: 塊①（`docs/spec-1-1-viewer.md`）・塊②（`docs/spec-1-2-tabs.md`）まで完了（2026-08-31）。
塊③-a（`docs/spec-1-3-reading.md`）まで完了（2026-09-01。テスト 248件・完了判定8項目すべて充足）。
塊③-b（`docs/spec-1-4-find-print.md`）まで完了（2026-09-01。テスト 307件。完了判定9項目のうち
8項目を実機で確認、残る1項目は Ctrl+P の OS ダイアログの目視でユーザー確認待ち）。
**塊④（`docs/spec-1-5-page-edit.md`）まで完了（2026-09-01。テスト 467件。完了判定8項目すべてを
実機で確認。人の目でしか確かめられない項目は無い）。**並べ替え・回転・削除・複数選択・
元に戻すと、未保存の表示・確認までが入った。**残るは塊⑤ の1本**（`docs/spec-1-6-save.md`。
2026-09-01 に事前調査と仕様の確定を終え、`claude/phase-1-save` で着手した）。

**2026-08-31 改訂（塊①の仕様確定に伴う3点）。**詳細は `docs/spec-1-1-viewer.md`。

| 変更 | 理由 |
|---|---|
| 「ファイルを開く」のダイアログ経路を②から①へ移した | ①に開く手段が1つも無いと表示を確認できず、「終われば何かが新しくできる」という PR の単位（決定7）を満たさない。ドラッグ＆ドロップ・タブ・最近使ったファイル・文書情報は②に残す |
| F-01-3（ページ移動）を①に足した | この要件はもともと5つの塊のどこにも割り当てられていなかった（取りこぼし）。連続スクロールを作れば現在ページの算出は必然的に要るため、前後移動と番号入力を足す差分は小さい |
| 見開き表示（F-01-1 後半）を①から外した | レイアウト計算とページモードの選択表示に波及する。常用開始（決定5）に必須ではないため、Phase 1 の完了時に実使用の不満を見てから位置を決める |

**塊⑤ に着手した（2026-09-01。`claude/phase-1-save` ブランチ）。**仕様書は `docs/spec-1-6-save.md`。
事前調査で「`utilityProcess` は asar の中のワーカーを fork でき、その中の `vendor/pdf-lib.min.js` も
`require` できる」ことを確かめ、最大の未知を先に潰した。

**暗号化 PDF の fixture（`docs/02` 第9章の論点5）は決着した。**pdf-lib は暗号化した書き出しに
対応しないが、**自前で生成できる**ことが実測で分かったため、qpdf は足さない（ユーザー確定 2026-09-01）。

**塊⑤ は割らずに1本の PR で仕上げる**（ユーザー確定 2026-09-01）。起草時は量と依存の向きを理由に
⑤-a／⑤-b の2本を推したが、Phase 1 の完了（＝常用開始）が一度に来ることを採った。

**2026-08-31 追記（塊②の実装で分かったこと）。**ドラッグ＆ドロップは「実機で人が
確かめるしかない」と見込んでいたが、Chromium の `Input.dispatchDragEvent` に実ファイルの
パスを渡せば `webContents.debugger` から再現できる。`SIGK_SMOKE_DROP` として起動確認に
組み込んだため、以降のフェーズでもドロップ経路は自動で確かめられる。

> **2026-09-02 実施済み。**結果と判定は `docs/01_製品要件定義.md` 4-1。測り口は
> `main.js` の `SIGK_SMOKE_PERF`、検体は `npm run fixtures:perf`。
> 目標に届かなかったのは「作り直した直後の初回起動」だけである。

**このフェーズの終わりに非機能要件を実測する。**起動時間・表示までの時間・メモリを計測し、`docs/01_製品要件定義.md` 第4章の目標値を実測値で見直す。目標に届かない場合、ここで対策を打つ（描画の遅延読み込み、サムネイルの解像度の見直し）。後のフェーズで機能を積んでからでは、原因の切り分けが難しくなる。常用を始める時期と重なるため、体感と数値を突き合わせられる。

---

## Phase 2 — 結合と分割

**目的**: PDF24 が担っている中核機能を、開いたまま使えるようにする。

| # | 作業 | 対応要件 |
|---|---|---|
| 2-1 | ツールモードの枠組み（サイドパネルにツール一覧、中央が作業画面に切り替わる） | — |
| 2-2 | 結合画面（一覧・ドラッグ並べ替え・ページ範囲指定） | F-03-1、F-03-2 |
| 2-3 | 分割画面（ページ数指定・位置指定・範囲抽出） | F-03-3〜F-03-5 |
| 2-4 | 出力先とファイル名規則の指定 | F-03-6 |
| 2-5 | 出力先の同名ファイルの確認 | `docs/04_UI設計.md` 第7章 |

---

## Phase 3 — 変換

**目的**: 画像と PDF を相互に変換できる。

| # | 作業 | 対応要件 |
|---|---|---|
| 3-1 | 画像 → PDF（用紙サイズ・向き・余白・1つにまとめる／個別） | F-04-1〜F-04-3 |
| 3-2 | PDF → 画像（ページ指定・解像度指定） | F-04-4 |

PDF → 画像は pdf.js でキャンバスに描いたものを書き出す。ワーカーは Node 側であり `canvas` を持たないため、この処理だけはレンダラーで行い、書き出しをメインへ渡す。**この非対称は設計の例外である**ため、`worker/op-convert.js` に理由をコメントで残す。

---

## Phase 4 — 注釈・テキスト・透かし

**目的**: 「読んで印をつけて返す」までを1つのアプリで完結させる。

| # | 作業 | 対応要件 |
|---|---|---|
| 4-1 | 日本語フォントの埋め込み基盤（fontkit・Noto Sans JP・サブセット化） | 前提 |
| 4-2 | ハイライト・下線・取り消し線 | F-05-1、F-05-2 |
| 4-3 | フリーテキスト | F-05-3 |
| 4-4 | 図形・フリーハンド | F-05-4、F-05-5 |
| 4-5 | ノート注釈 | F-05-6 |
| 4-6 | 注釈一覧とプロパティパネル | F-05-7 |
| 4-7 | テキスト透かし・画像透かし | F-05-8、F-05-9 |
| 4-8 | フラット化（確認ダイアログ付き） | F-05-10 |

4-1 を最初に置くのは、日本語が入らない状態で 4-3・4-7 を作っても実用にならないためである（`docs/02_アーキテクチャ設計.md` 第1-4章）。

---

## Phase 5 — シェル統合と配布

**目的**: 右クリックメニューから使える。インストーラーで配れる。

| # | 作業 | 対応要件 |
|---|---|---|
| 5-1 | コマンドライン引数の解釈（`merge`・`split`・`toPdf`。`open` は Phase 1 で済み） | F-07-2〜F-07-4 |
| 5-2 | 引数の集約（`PENDING_WINDOW_MS` の実測と確定。単一インスタンス自体は Phase 1 で済み） | `docs/03` 第3-2章 |
| 5-3 | `build/installer.nsh`（レジストリの登録・削除） | `docs/03` 第4章 |
| 5-4 | 「プログラムから開く」への登録と既定のアプリ設定への導線 | F-07-5 |
| 5-5 | Windows 10 / 11 実機での確認（`docs/03` 第5章の11項目） | — |
| 5-6 | `docs/インストールと使い方ガイド.md` | — |
| 5-7 | アプリアイコン（`assets/icon.ico`） | — |

5-2 と 5-5 は実機がないと確定しない。~~開発環境が Linux コンテナである間は着手できない~~ **2026-08-31: 開発環境が Windows 実機になったため、着手の制約は解消した。**ただし作業順としては Phase 5 に置いたままとする。Phase 1 で前倒しするのは `open` の経路（パスが届くたびにタブを足す）だけであり、集約の待ち時間を測る必要はない。

---

## Phase 6 — 仕上げ

| # | 作業 |
|---|---|
| 6-1 | エラーのローカルログの見直し（実装自体は Phase 0 で新規に済んでいる） |
| 6-2 | 設定画面（既定の保存先・`.bak` の作成の有無。`.bak` の実装自体は Phase 1 で入る） |
| 6-3 | 壊れた PDF・巨大な PDF・暗号化 PDF での異常系の確認 |
| 6-4 | `THIRD-PARTY-NOTICES.md` の最終確認 |
| 6-5 | `docs/06_ライセンス・商標チェックリスト.md` の全項目を通す |
| 6-6 | v1.0.0 のインストーラー生成 |

---

## 第2版以降（未着手）

`docs/01_製品要件定義.md` 第3-2章の機能群。着手の前に、それぞれ単独で仕様を確定させる。

| 機能 | 先に決めるべきこと |
|---|---|
| 圧縮 | Ghostscript の同梱可否。AGPL であるため、配布形態によってはソース公開義務が生じる。**ユーザーの判断が要る** |
| OCR | Tesseract と日本語学習データの同梱。インストーラーのサイズが数十MB増える |
| パスワード保護 | pdf-lib は暗号化した書き出しに対応しない。代替ライブラリの選定が要る |
| 墨消し | テキストオブジェクトの確実な削除を、どう検証するか |
| フォーム入力 | AcroForm の対応範囲 |
| Windows 11 の新メニュー | `docs/03` 第6章 |

---

## 進め方についての注記

- 各フェーズの着手前に、そのフェーズの仕様書を `docs/spec-<phase>-<id>-<name>.md` として書き、確定させてから実装に入る（CheckListMaker と同じ命名）。
- フェーズをまたいで機能を先取りしない。Phase 1 の途中で注釈を作り始めない。**ただし Phase 1 の前倒し2件は例外**であり、常用開始という目的から逆算した意図的な繰り上げである。
- 各フェーズの終わりにインストーラーを1本作り、実機で触る。触ってからでないと分からない問題（右クリックの挙動、日本語の描画、大きなファイルの体感速度）が多いためである。
- **Phase 3 以降の順序と中身は、Phase 1 の常用で出た不満を根拠に見直す**（`docs/07_開発計画の決定事項.md` 決定5）。着手前に書いた要件定義の推測より、毎日使って出た不満のほうが優先度の根拠として確かである。

### 検証をどこで回すか

**2026-08-31 改訂。** 開発環境が Windows 11 実機になり、`npm install` も Electron の起動も
インストーラーの生成も手元でできるようになった。かつての三段構え（手元・CI・実機）は
手元と実機が同じ場所になったため、二段に改める（決定1・17）。

| 段 | 何を見るか | 場所 |
|---|---|---|
| 手元 | `npm install` / `npm test` / `npm start` / `npm run dist` のすべて。体感速度、日本語の描画、右クリックの挙動、`app://` の実動作 | Windows 実機 |
| CI | 依存を新規に解決するクリーンな環境でも同じ結果になること。インストーラーの Artifact 化 | GitHub Actions |

push する前に手元で4つのコマンドをすべて通す。CI を試行錯誤の場にしない。

**2026-08-31 追記。`npm run dist` は「生成できたか」までしか見ていない。生成物を起動する
ところまでを検証に含める。**

```
SIGK_SMOKE=1 "./dist/win-unpacked/SigK PDF.exe"
```

塊② で `package.json` の `build.files` に `recent-documents.js` を書き忘れ、**インストーラー
から入れたアプリだけが起動時に落ちる**状態のまま完了と判定していた（`docs/spec-1-2-tabs.md`
の不具合3）。`npm test` も `npm run dist` も緑で、実測でも表に出なかった。手元で回すのは
開発ツリーの `npm start` であり、配布物の `asar` は別物だからである。

`installSmokeCheck` は `app.isPackaged` で塞いでいないので、配布物にも `SIGK_SMOKE` がそのまま
効く。**この起動を各塊の完了判定に入れる。**取りこぼしの機械的な検出は
`test/dist-files.test.js` が担う（`main.js`・`preload.js` から辿れる `require` と `index.html`
が読むものが、すべて `build.files` に載っているか）。

**2026-09-01 追記。`SIGK_SMOKE_DISPLAY=<番号|secondary>` を付けると、起動確認の窓を指定した
ディスプレイの中央へ寄せられる。**確認のたびに作業中の画面へ窓が出てくるのを避けるためである。
移した位置は `settings.json` に残り、次からは通常の起動もそちらに出る（ユーザー判断）。

```
SIGK_SMOKE=1 SIGK_SMOKE_DISPLAY=secondary SIGK_SMOKE_PDF=test/fixtures/text-heavy.pdf SIGK_SMOKE_FIND=invoice npx electron .
```

### PR の刻み

- 「終われば何かが新しくできるようになっている」単位で PR を切る（決定7）。作業項目ごとには刻まない。フェーズ丸ごと1本にもしない。
- 作業ブランチで進め、**PR の作成とマージはユーザーの明示指示を待つ**（`.claude/CLAUDE.md` 領域3）。
- リポジトリ設定の「Automatically delete head branches」を有効にしてある。マージ済みのブランチは自動で消えるため、刻んでもブランチが溜まらない（領域6 の制約への対処）。
