# 仕様書: Phase 1 塊④ ページ編集

- ステータス: **実装済み**（2026-09-01 起草・同日確定・同日実装。確定事項58件。論点4件はユーザー決定済み → 末尾「ユーザーの確定」）
- 関連: `docs/05_開発ロードマップ.md` Phase 1 塊④ ／ `docs/spec-1-3-reading.md` ／ `docs/spec-1-4-find-print.md` ／ `docs/04_UI設計.md` 4-2・第7章・第8章 ／ `docs/02_アーキテクチャ設計.md` 2-3
- 対応要件: F-02-1（並べ替え）／F-02-2（回転）／F-02-3（削除）／F-02-6（複数選択）／F-02-7（元に戻す・やり直し）
- 実装: `renderer/page-plan.js`・`renderer/page-history.js`・`renderer/page-grid.js`・`renderer/page-edit.js`・`renderer/confirm-discard.js`（新規）／`renderer/thumbnails.js`・`renderer/viewer.js`・`renderer/viewer-layout.js`・`renderer/page-render.js`・`renderer/print.js`・`renderer/tabs.js`・`renderer/viewer-controls.js`・`index.html`・`renderer/shell.css`・`assets/icons.js`・`main.js`・`preload.js`・`renderer/app.js`（改修）
- テスト: `test/page-plan.test.js`・`test/page-grid.test.js`・`test/page-edit.test.js`・`test/confirm-discard.test.js`（新規）／`test/harness.js`・`test/shell.test.js`・`test/viewer-layout.test.js`・`test/thumbnails.test.js`・`test/fixtures/build.js`（改修）

**起草時の予定から2つ増えた。**`page-plan.js` が 328 行になったので undo/redo を
`page-history.js` へ出した（plan を受け取って plan を返すだけで、並べ替えや回転を
知らない層である）。未保存の確認は `confirm-discard.js` に分けた（タブを閉じるときと
アプリを終了するときの2か所から呼ばれ、tabs.js に置くと責務が混ざる）。
`renderer/find.js`・`renderer/shell.js` は手を入れずに済んだ。

## 目的

塊③-b までで **読む**側は揃った。開いて、並べて、探して、紙に出せる。
ここから先は **直す**側である。塊④ はその第一歩で、**ページの並びを組み替える**ところまでを担う。

- **ページモードが空である。**ツールレールの「ページ」を押すとタイトルが変わり、
  サイドパネル下部に操作ボタンが4つ出るが、**4つとも `aria-disabled` のまま**で、
  しかもサムネイル自体が消える（閲覧モード専用にしてあるため）。何もできない。
- **`ops` を組み立てる場所が無い。**`docs/02` 2-3 は編集内容の持ち方を定めているが、
  実装は1行も無く、後述のとおり**設計に穴がある**。
- **Ctrl+Z / Ctrl+Y / Delete は `docs/04` 第8章で予約済みだが動かない。**

塊④ で「並べ替え・回転・削除・複数選択・元に戻す」を入れ、**編集した結果を画面に映す**。
ファイルへ書き戻すのは塊⑤ である。

---

## 含めるもの / 含めないもの

| 含める | 含めない |
|---|---|
| ページモードのサイドパネル（多列グリッド） | ファイルへの保存（塊⑤。F-06-2） |
| ドラッグによる並べ替え（F-02-1） | **抽出（F-02-4）・挿入（F-02-5）→ 塊⑤へ送る**（決定1） |
| 90度単位の回転（F-02-2） | **矩形選択（F-02-6 の後半）→ 入れない**（決定2） |
| 削除（F-02-3） | 見開き表示（F-01-1 後半。`docs/07` 第6章のまま） |
| Ctrl / Shift クリックによる複数選択（F-02-6 の前半） | ページ内のテキスト・注釈の編集（Phase 4 以降） |
| 元に戻す・やり直し（F-02-7） | `.bak`・上書き保存・名前を付けて保存（塊⑤） |
| **編集結果を保存前に画面へ映す**（ページビュー・ページ番号・印刷・サムネイル） | ワーカー（`utilityProcess`）と進捗・中止（塊⑤） |
| **未保存（dirty）の表示と、閉じるときの2択の確認**（決定3） | 「保存」を含む3択の確認（塊⑤。F-06-5 の残り） |
| | 暗号化 PDF・壊れた PDF の編集可否の判定（塊⑤） |

---

## 事前調査

仕様を決める前に、**実物のコードと実物の pdf-lib で確かめた**こと。推測で書いた設計を
実装で覆さないためである（塊③-b で同じ手順が効いた）。

### 既存コードの実測

| 調べたこと | 分かったこと |
|---|---|
| ページモードは今なにをしているか | `shell.js` の `setMode()` が `data-mode` 属性・レールの `active`・`#side-title`・`#side-actions.hidden` の4つを変えるだけ。さらに `thumbnails.refresh()` を呼ぶが、閲覧モード以外では `discard()` されるため**サムネイルは消える** |
| サイドパネルの操作ボタン | `#side-actions` に **`div.act` が4つ**（左回転・右回転・抽出・削除）。`id` が無く、`button` でもなく、`tabindex` も `role` も無い。**有効にしてもキーボードから押せない**。`.side-actions` は `grid-template-columns:1fr 1fr` の2列固定 |
| サムネイルの多列化 | `layoutThumbnails()` は**1列専用**。列数の引数が無く、返す `page` に `left` が無い。`.thumb{left:0}`（`shell.css:133`）も1列前提。直す箇所は `viewer-layout.js`・`thumbnails.js`・`shell.css` の3つ |
| ページ選択という概念 | **どこにも無い。**「現在のページ」（`current`、単数）しか無い |
| ドラッグ並べ替えの下地 | 無い。`file-drop.js` は**ファイルのドロップ専用**で、`types.includes('Files')` で判定しているため、**ページ内どうしのドラッグではオーバーレイは出ない**（干渉しない）。ポインタ操作の作法は `shell.js` のサイドパネル用つまみに前例がある |
| ページ数の出どころ | `viewer.getState().pageCount` は `doc.numPages` ではなく **`state.sizes.length`**。`state.sizes` を編集後の並びで作り直せば、ページ番号入力・Home/End・印刷範囲・検索の走査本数は**自動で追従する** |
| `doc.getPage()` を直に呼ぶ場所 | **`page-render.js:83` と `thumbnails.js:158` の2か所だけ**。`find.js` と `print.js` は `viewer.getPage(number)` の1本を通っている。つまり写像を入れる場所は実質3か所 |
| ステータスバーのページ数 | `open()` の中で `doc.numPages` から**1回だけ**書き、以後どこも更新しない。編集しても取り残される（要修正） |
| サムネイルの持ち物 | `thumbnails.js` は `setDocument({doc, sizes, ...})` で **doc と sizes の写しを自前に持つ**。公開 API 7本に「並びだけ差し替える」口が無く、`setDocument` を呼び直すとスクロール位置と描画済みを全部捨てる（口を1本足す） |
| 検索の状態 | `find.js` の `state.pages` と `matches[].page` は**ページ index**。編集で一斉にずれるが、無効化する入口が `clear()`（文書を閉じるときだけ）しか無い |
| 開いたバイト列 | pdf.js の `getDocument` は `data` の `ArrayBuffer` を **transferable としてワーカーへ渡す**。`viewer.open()` の時点でレンダラー側の `Uint8Array` は detach され、**どこにも残らない**。保存はパスから読み直すことになる（塊⑤） |
| キーの空き | メニューは「ファイル」「ヘルプ」だけで **`editMenu` ロールを持たない**ので、Ctrl+Z / Ctrl+Y / Ctrl+A はレンダラーの `keydown` に届く。ただし `viewer-controls.js` の `handleKey` は `event.ctrlKey` で早期 return するため、いまは素通りしている |
| アイコン | 20個。`rotateLeft`・`rotateRight`・`extract`・`trash` はある。**`insert`・`undo`・`redo` が無い** |
| pdf-lib の読み込み | `index.html` の `<script>` 19本に **`vendor/pdf-lib.min.js` は入っていない**（525KB）。塊④ はレンダラーで pdf-lib を使わないので、読み込まないままにできる |

### 回転を画面へ映せるか（pdf.js 側）

| 調べたこと | 分かったこと |
|---|---|
| `getViewport` は回転を受けるか | **受ける。**既定値はページ自身の `rotate`。渡した値は**絶対値として置き換える**。`PageViewport` が 360 で剰余し、負値に +360 するので `page.rotate + 90` や `- 90` をそのまま渡してよい。**90 の倍数でない値だけ例外**になる（pdf.js は `rotate` を 90 の倍数に正規化して持つので、実際には起きない） |
| `getViewport({scale})` を回転なしで呼んでいる場所 | **4か所。**`page-render.js` の描画、`page-render.js` のテキストレイヤー、`thumbnails.js` のサムネイル、**`print.js` の 150dpi 画像**。4つ目を忘れると「画面では回っているのに印刷は回っていない」が起きる |

### pdf-lib 側（塊⑤で効くが、いま決めておくべきもの）

実物の pdf-lib 1.17.1 を動かして確かめた。**塊④ ではファイルへ書かないが、
書けない設計を塊④ で作らないため**に先に調べた。

| 調べたこと | 分かったこと |
|---|---|
| 同一文書内の並べ替え | `removePage` ＋ `insertPage` で、注釈・リンク・しおりが保たれる。`/Names` の宛先も保たれる |
| `PageLabels` | **追従しない。**並べ替えても catalog の `/PageLabels` はそのまま残るため、ラベル付き文書では保存後にラベルがずれる。本アプリの画面は 1..N の連番なので**画面上は分からず、他のビューアで開いたときだけ見える** |
| 削除でファイルは小さくなるか | **ならない。**1,000ページ・195.7MB から 500ページ削除して保存しても、出力は 195.7MB のまま |
| `copyPages` での複製 | しおり（outlines）と AcroForm が**必ず消える**。挿入では、運ばれた内部リンクの飛び先がページ木の外を指し、**壊れたリンクと余分なページオブジェクトが混入する** |
| `setRotation` | **絶対値**で、正規化しない。0/90/180/270 以外や負値をそのまま入れられる。pdf.js の扱いと食い違うので、**正規化は1か所で1回だけ**行う決めが要る |
| 全ページ削除 | `save()` の既定 `addDefaultPage:true` により、**黙って白紙 A4 が1枚生える** |
| メタデータ | 既定 `updateMetadata:true` で Producer が pdf-lib に書き換わる |
| 【重大】和文フォーム PDF | `save()` の既定 `updateFieldAppearances:true` が、`/AP` を持たない欄の見た目を **Helvetica（WinAnsi）で作り直す**。日本語の値が入った欄があると、**ページを1枚消しただけで `WinAnsi cannot encode "氏"` で保存が失敗する**。`save({updateFieldAppearances:false})` なら成功し、値も保たれる |
| 進捗・中止 | `save()` の引数は4つだけで、**進捗のコールバックも中止の手段も無い**。`docs/04` 第6章の「1秒を超えそうな処理は進捗と中止」に対し、進捗は save の中ではなく**外側（読み込み／適用／書き出しの3段）でしか出せない** |
| 想定上限での実測 | 1,000ページ・195.7MB で load 526ms・全ページ逆順の並べ替え 16ms・save 1,634ms。rss は読み込み直後 251MB、save 直後 470MB、続けて2本目を扱うと 678MB |
| pdf.js で開けて pdf-lib で開けない文書 | **ある。**内容が欠損した PDF では `load` が例外になり、しかも素の `TypeError`（`Cannot read properties of undefined (reading 'Pages')`）が混じる。警告は `console.warn` へ直接出るため `log:error` に載らない |

**この節の結論**: **並べ替え・回転・削除そのものは 16ms で終わる。**重いのは読み込みと書き出しで、
どちらも塊⑤ の担当である。塊④ を「画面の上の計画づくり」に閉じれば、
pdf-lib をレンダラーへ持ち込まずに済む。

---

## 確定事項

### A. 編集内容の持ち方（`docs/02` 2-3 の改訂）

`docs/02` 2-3 は編集を **`ops`（操作の列）** で持つと定めている。実物に当てて調べたところ、
**4つの穴**があった。

1. `reorder` / `rotate` / `delete` のページ番号が**いつ時点のものか**が定義されていない。
   `delete` の後の `rotate` は削除前の番号か、削除後の番号か。
2. `rotate` の `degrees` が**絶対値か相対値か**が定義されていない。
   pdf.js（絶対）と pdf-lib（絶対）に対し、UI は「右へ90度」という相対の操作である。
3. **抽出・挿入の op が無い。**
4. **undo が書けない。**`docs/02` は「undo/redo が操作の列の巻き戻しとして自然に書ける」と
   するが、各 op に逆操作が無く、ページの同一性（どの op のどのページが同じページか）も
   無いため、列を1つ削っても元の状態には戻らない。

| # | 論点 | 決定 |
|---|------|------|
| 1 | 編集の表し方 | **「操作の列」ではなく「結果の並び」で持つ。**編集の状態は `plan` という配列1本で、要素は `{ src, rotate }`。`src` は**元ファイルの 0 始まりページ番号**、`rotate` は**元ページの `/Rotate` に足す相対角度**（0/90/180/270）。表示・印刷・保存はすべてこの配列だけを見る |
| 2 | なぜ結果の並びか | ① 時点の曖昧さが消える（配列は常に「いま」を表す）② undo がスナップショット列で書ける ③ 適用が冪等（同じ plan を何度当てても同じ）④ 並べ替え・回転・削除はすべて配列の操作に落ちる |
| 3 | `docs/02` 2-3 の扱い | **`ops` を捨てるのではなく、住み分ける。**ページの並び（並べ替え・回転・削除・抽出・挿入）は `pages`（＝ plan）で、ページに**足す**もの（注釈・透かし）は従来どおり `ops` で送る。ワーカーへ渡す形は `{ source, pages, ops, target }`。**`docs/02` 2-3 をこの形へ改訂する**（決定4） |
| 4 | 回転の正規化 | **`page-plan.js` の1か所だけで行う。**`rotate` は常に 0/90/180/270 に丸めて持つ。画面へ渡すときは `page.rotate + plan[i].rotate`（pdf.js が 360 で剰余する）。保存時は `((元 + 相対) % 360 + 360) % 360` を `setRotation` へ渡す |
| 5 | plan の初期値 | 文書を開いた時点で `[{src:0,rotate:0}, {src:1,rotate:0}, ...]`。**編集していない状態も plan で表す**（特別扱いを作らない） |
| 6 | dirty の判定 | plan が初期値と一致しなければ dirty。**配列の比較で決める**（操作した回数では決めない。3回回して元に戻したら dirty ではない） |
| 7 | plan の置き場所 | **タブごと**（`spec-1-2` 確定事項2 の session）。タブを切り替えても編集内容が残る |

### B. 元に戻す・やり直し（F-02-7）

| # | 論点 | 決定 |
|---|------|------|
| 8 | 持ち方 | **plan のスナップショット列。**`{ stack: [plan0, plan1, ...], at: n }` を持ち、Ctrl+Z で `at` を1つ戻し、Ctrl+Y で1つ進める。逆操作を書かないので、操作の種類が増えても undo の実装は増えない |
| 9 | 履歴の上限 | **50世代。**超えたら古いほうから捨てる。plan 1本は1,000ページでも要素1,000個（数十KB）なので、50世代持っても数MBに収まる |
| 10 | 履歴の分岐 | 戻した状態から新しい操作をしたら、**先の履歴は捨てる**（一般的な作法） |
| 11 | 履歴の範囲 | **タブごと。**別のタブの操作は混ざらない。文書を閉じたら履歴も捨てる |
| 12 | 選択は履歴に入れるか | **入れない。**選択の変更では undo できない。ただし戻した直後の選択は、**その世代で操作の対象だったページ**に合わせる（何が戻ったか分かるようにする） |
| 13 | 履歴に積む単位 | 1回のユーザー操作で1世代。ドラッグ1回の並べ替え、回転ボタン1回、削除1回がそれぞれ1世代 |

### C. 選択（F-02-6）

| # | 論点 | 決定 |
|---|------|------|
| 14 | 持ち方 | **表示上の index の集合**（`Set`）と、Shift クリックの起点（`anchor`）。`src` ではなく表示 index で持つ（並べ替えで選択が飛ばないようにするため、並べ替え時は移動先の index へ付け替える） |
| 15 | クリック | 単独クリックはその1枚だけを選び、`anchor` をそこへ置く |
| 16 | Ctrl クリック | 押した1枚の選択を反転する。`anchor` をそこへ移す |
| 17 | Shift クリック | `anchor` からそこまでの範囲を選ぶ（`anchor` は動かさない）。Ctrl+Shift は範囲を**足す** |
| 18 | Ctrl+A | **ページモードで、かつサイドパネルにフォーカスがあるときだけ**全ページを選ぶ。それ以外は Chromium 既定の全選択（テキストレイヤー）に渡す。奪いすぎると閲覧モードで文字を選べなくなる |
| 19 | Esc | 選択を解除する。**検索バーが開いているときは検索バーを閉じるほうを優先する**（`docs/04` 第8章が Esc に2つ割り当てているため、優先順位をここで決める） |
| 20 | 矩形選択 | **入れない**（第1版）。左ドラッグは並べ替えに割り当て済みで操作が競合し、`docs/04` 4-2 にも記述が無い（決定2） |
| 21 | 選択とページビュー | 選択が1枚になったら、ページビューをそのページへ寄せる（`docs/04` 4-2 の「選択中のページへ追従する」）。複数選択のときは動かさない |
| 22 | 選択の見た目 | `.thumb.selected` を足す。枠は `--accent`、地は `--select`。**現在ページの `.current` とは別に持つ**（両方付くことがある） |

### D. サイドパネルの多列グリッド

| # | 論点 | 決定 |
|---|------|------|
| 23 | 列数 | **パネルの幅から自動で決める。**`列数 = 1〜3 の範囲で floor((内容幅 + 10) / 110)`（**実装の除数は 110 ではなく 100**。この行の下の訂正を見ること）。目標の紙幅は 100px。実測値は 幅180px→1列（紙150px）／240px→2列（95px）／300px→2列（125px）／340px→3列（90px）／420px→3列（117px）。固定の2列にすると最小幅で紙が 65px まで縮む<br>**2026-09-01 実装時に訂正: 除数は 110 ではなく 100。**この実測表は縦スクロールバー（約16px）を勘定に入れておらず、実機の内容幅は表より 16px 狭い。列数は表のとおりになるが、紙幅は小さくなる（1列 −16px／2列 −8px／3列 −6px。→「実測で直した前提」） |
| 24 | なぜ自動か | サイドパネルは 180〜420px でドラッグできる（`shell.js`）。固定列にすると、どこかの幅で必ず破綻する |
| 25 | `layoutThumbnails` の改修 | 引数に `columns` を足し、返す `page` に `left` を足す。**1列のときの返り値は今と同じ**にする（閲覧モードの回帰を防ぐ） |
| 26 | 同時に持つサムネイルの上限 | **`24 × 列数`**（1列24・2列48・3列72）。多列では紙が小さくなるため、枚数が増えても総メモリは1列時を下回る（幅240pxで 1列210px 対 2列105px ＝ 面積比4倍）<br>**2026-09-01 実装時に補足: 先読みも列数倍にした。**先読みは枚数で数えるため、3列のまま 4 では 1.3 行ぶんにしかならず、上限に遠く届かない（実測17枚）。列数倍にして33枚（→「実測で直した前提」） |
| 27 | 列数が変わったとき | 枠を作り直し、描画済みを捨てる。パネルの幅変更で既に同じ経路が動いている（`spec-1-3` 確定事項15） |
| 28 | 閲覧モードのサイドパネル | **変えない。**1列のまま。ページモードだけ多列にする |
| 29 | モードを切り替えたとき | サムネイルを捨てずに**並べ直す**。`thumbnails` に `setPlan(plan)` と `setColumns(n)` を足し、`setDocument` の呼び直し（スクロール位置と描画済みを全部捨てる）を避ける |

### E. 並べ替え（F-02-1）

| # | 論点 | 決定 |
|---|------|------|
| 30 | 方式 | **ポインタイベント**（`pointerdown` / `pointermove` / `pointerup`）。HTML5 の `draggable` にしない |
| 31 | なぜポインタか | ① jsdom は `DragEvent` も `DataTransfer` も持たないため、HTML5 方式は画面テストにまったく載らない。ポインタ方式なら**挿入位置の計算を純粋関数に切り出せて**、依存なしで検証できる ② 既存の `SIGK_SMOKE_DROP` と同じ `webContents.debugger` から `Input.dispatchMouseEvent` を送れば起動確認を機械化できる見込み ③ `shell.js` のつまみに前例がある。なお `file-drop.js` は `types.includes('Files')` で判定しているので、**どちらを選んでもファイルのドロップとは干渉しない** |
| 32 | 掴む閾値 | 5px 動いてから並べ替えを始める。クリックとの取り違えを防ぐ |
| 33 | 挿入位置の決め方 | **純粋関数 `dropIndex({ layout, columns, x, y })`。**y から行、x から列を出し、その枠の中心より左（上）なら手前、右（下）なら後ろへ入れる。**`elementFromPoint` を使わない**（jsdom が持たないため） |
| 34 | 複数枚のドラッグ | 選択中の枚数をまとめて動かす。**掴んだ枚数を示すバッジ**を出す。掴んだ枚が選択に含まれていなければ、掴んだ1枚だけを選び直してから動かす |
| 35 | 挿入位置の表示 | 挿入先に**縦棒**（`--accent`、幅2px）を出す。掴んでいる枚は半透明にする |
| 36 | ドラッグ中の自動スクロール | パネルの上下端 40px に入ったらスクロールする。長い文書で端まで運べないのを防ぐ |
| 37 | 取り消し | ドラッグ中の Esc、またはパネルの外での `pointerup` で取り消す。plan は変えない |

### F. 回転（F-02-2）と削除（F-02-3）

| # | 論点 | 決定 |
|---|------|------|
| 38 | 回転の単位 | **90度単位。**左回転（−90）と右回転（+90）の2つ。選択中のページすべてに掛ける。選択が無ければ現在のページに掛ける |
| 39 | 回転の見せ方 | **その場で画面へ映す。**サムネイル・ページビュー・テキストレイヤー・**印刷用の画像**の4か所すべてへ届ける（事前調査で挙げた `getViewport` の4か所） |
| 40 | 削除の確認 | **出さない。**`docs/04` 第7章のとおり。**ただしその根拠は「Ctrl+Z で戻せる」ことなので、確定事項8（スナップショット方式の undo）が実装されて初めて成立する**。undo を落とすなら確認を出す側へ倒す |
| 41 | 全ページの削除 | **最後の1ページは消せない。**選択が全ページのときは削除ボタンを無効にし、Delete も効かないようにする。pdf-lib の `addDefaultPage` が黙って白紙 A4 を生やす件（事前調査）を、そもそも起こさない |
| 42 | 削除後の選択と現在ページ | 削除した位置に来たページを選び直す。末尾を消したなら最後のページ。**選択が空にならないようにする**（続けて Delete を押せる） |

### G. 編集結果を画面へ映す

| # | 論点 | 決定 |
|---|------|------|
| 43 | 映すか映さないか | **映す。**ページビュー・ページ番号・サムネイル・印刷のすべてが編集後の並びを示す。映さないと「サイドパネルだけが新しい並び、中央と印刷は元のまま」という二重状態になり、塊⑤ の保存が正しいかを目で確かめられない |
| 44 | 写像を入れる場所 | **3か所。**`viewer.getPage(表示番号)` が `plan[表示番号-1].src` を引いて元ページを返す。直に `doc.getPage()` を呼んでいる `page-render.js` と `thumbnails.js` も同じ写像を通す。`find.js` と `print.js` は既に `viewer.getPage()` 経由なので**手を入れない** |
| 45 | `state.sizes` | plan から作り直す。回転が 90/270 のときは幅と高さを入れ替える。`pageCount` は `state.sizes.length` なので、ページ番号入力・Home/End・印刷範囲・検索の走査本数が**自動で追従する** |
| 46 | ステータスバーのページ数 | **編集のたびに更新する。**いまは `open()` で `doc.numPages` から1回書くだけで取り残される（既存の取りこぼし） |
| 47 | 検索結果の扱い | **編集したら捨てる**（`find.clear()` を呼ぶ）。`matches` はページ index とページ内オフセットを持つため、並べ替えなら付け替えられても削除・挿入では正しく直せない。**半分だけ正しいハイライトは、無いより悪い** |
| 48 | 印刷の対象 | **編集後の並び。**確定事項44 の写像を通るので自動的にそうなる。回転も 150dpi の画像に載る（確定事項39） |
| 49 | 未保存の表示 | タブに点（`.tab .dirty` の CSS は既にある。実装が無い）とステータスバーの「変更あり」を出す（決定3）。閉じるときの確認は確定事項56〜58 |

### H. 画面の骨組みとキー

| # | 論点 | 決定 |
|---|------|------|
| 50 | `#side-actions` の作り替え | `div.act` → **`button.act`**（`type="button"`）にし、`id` を付ける。いまはフォーカスも当たらずキーボードから押せない |
| 51 | 並べる操作 | **5つ。**左回転・右回転／抽出・挿入／削除。`grid-template-columns:1fr 1fr` のまま3行にし、**削除だけ `grid-column:1 / -1` で全幅**にして `--danger` で塗る。抽出と挿入は**枠だけ置いて `aria-disabled` のままにする**（決定1。結線は塊⑤） |
| 52 | サムネイル上のボタン | ホバーで回転・削除の小さなボタンを出す（`docs/04` 4-2）。**紙の幅が 100px 前後になるため、アイコンのみ・16px** にする |
| 53 | 元に戻す・やり直しの置き場所 | **ツールバー**の左寄り（開く の隣）。ページモード以外でも押せるようにするが、編集が無ければ無効 |
| 54 | キー | Ctrl+Z（元に戻す）／Ctrl+Y（やり直し）／Delete（削除）／Ctrl+A（全選択）／Esc（選択解除）。**`viewer-controls.js` の `handleKey` は `event.ctrlKey` で早期 return しているので、塊③-b の `handleFindPrintKey` と同じく、その手前に分岐を置く** |
| 55 | ページモード以外での扱い | Delete と Ctrl+A はページモードでだけ効かせる。Ctrl+Z / Ctrl+Y は**どのモードでも効かせる**（編集したまま閲覧モードへ戻ることがある） |

### I. 未保存（dirty）の扱い（決定3）

| # | 論点 | 決定 |
|---|------|------|
| 56 | 塊④ に入れる範囲 | **表示と、閉じるときの確認まで。**dirty になる経路は塊④ で初めて生まれるため、ここを空けたままにすると「編集したタブを閉じると、確認も出ずに編集が消える」状態で塊④ が終わる |
| 57 | 確認の中身 | **2択。**「編集内容は保存されていません。閉じると失われます」＋「閉じる」（`--danger`）／「キャンセル」。既定のフォーカスは安全側（キャンセル）に置く（`docs/04` 第7章）。意匠は文書情報・印刷ダイアログと同じ `<dialog>` の作法にする |
| 58 | 「保存」を含む3択 | **塊⑤ で完成させる。**塊④ には保存の手段がまだ無く、選ばせても行き先が無い。`docs/04` 第7章が定める3択（保存／保存しない／キャンセル）と F-06-5 の残りは塊⑤ のまま |

出す場面は2つ。**タブを閉じるとき**（`Ctrl+W`・タブの ×）と、**アプリを終了するとき**
（`close` イベント。dirty なタブが複数あれば1つずつ聞く）。文書を開き直すときは、
いまの作りでは新しいタブになるので対象外である。

---

## 足りない部品

| 部品 | 内容 |
|---|---|
| `assets/icons.js` の `insert` | 挿入。24×24・線幅1.75・幾何形状（`docs/06` 2-2） |
| `assets/icons.js` の `undo` | 元に戻す。左向きの弧＋矢じり |
| `assets/icons.js` の `redo` | やり直し。`undo` の左右反転 |
| `layoutThumbnails` の `columns` と `left` | 多列グリッド（確定事項25） |
| `thumbnails.setPlan()` / `setColumns()` | 並びと列数だけを差し替える口（確定事項29） |
| `viewer` の plan 経路 | `getPlan()` / `applyPlan(plan)`。`detach()`／`attach()` の session にも積む |
| `.thumb.selected` ／ 挿入位置の縦棒 ／ ドラッグ中のバッジ | `renderer/shell.css` |
| ステータスバーの「変更あり」 | `index.html` に要素が無い |
| タブの点の実装 | `.tab .dirty` の CSS はあるが `tabs.js` に実装が無い |
| 未保存の確認ダイアログ | `index.html` に `<dialog id="confirm-discard">`。文書情報・印刷ダイアログと同じ作法（確定事項57） |

### 新しいモジュール

`docs/07` 第4章のテスト二層に合わせ、**依存の要らない層を必ず切り出す**。

| ファイル | 層 | 中身 |
|---|---|---|
| `renderer/page-plan.js` | **純粋層**（依存なし） | plan の生成・並べ替え・回転・削除、dirty の判定、undo/redo の履歴、選択の集合演算（Ctrl / Shift クリックの解決）、`dropIndex()` |
| `renderer/page-grid.js` | 描画層 | 多列グリッドの組み立て、選択の見た目、ドラッグの結線、挿入位置の表示、自動スクロール |
| `renderer/page-edit.js` | 適用層 | plan を viewer・thumbnails・find・ステータスバー・タブへ配る。`#side-actions` とキーの結線 |

---

## 画面の動き

| 操作 | 結果 |
|---|---|
| ツールレールの「ページ」 | サイドパネルが多列グリッドに変わる。列数はパネルの幅で決まる（1〜3列） |
| サムネイルをクリック | その1枚を選ぶ。ページビューがそのページへ寄る |
| Ctrl クリック | その1枚の選択を反転する |
| Shift クリック | 直前の起点からそこまでを選ぶ |
| Ctrl+A | ページモードでサイドパネルにフォーカスがあれば全ページを選ぶ |
| サムネイルをドラッグ | 5px 動いた時点で掴む。挿入先に縦棒が出る。複数選んでいればまとめて動き、枚数のバッジが出る |
| ドラッグ中に端へ寄せる | パネルが自動でスクロールする |
| ドラッグ中に Esc | 取り消す。並びは変わらない |
| 「右回転」 | 選択中のページが 90 度回る。**サムネイル・ページビュー・印刷のすべてで回る** |
| 「削除」または Delete | 選択中のページが並びから消える。**確認は出ない**。次のページが選ばれる |
| 全ページを選んで「削除」 | ボタンが無効。Delete も効かない（最後の1枚は消せない） |
| Ctrl+Z | 直前の操作を取り消す。そのとき操作の対象だったページが選ばれる |
| Ctrl+Y | 取り消しをやり直す |
| 編集した | タブに点が付き、ステータスバーに「変更あり」が出る |
| 編集した状態で Ctrl+F | **検索結果は捨てられている**。打ち直すと編集後の並びで探す |
| 編集した状態で Ctrl+P | 編集後の並び・回転で印刷する |
| 別のタブへ移って戻る | 編集内容・選択・履歴がそのまま残っている |
| 閲覧モードへ戻る | サイドパネルは1列に戻る。**編集後の並びのまま** |

---

## テストの範囲

jsdom は CSS を解釈せず `getBoundingClientRect()` が 0 を返す。**位置と見た目は起動確認で見る**
（`spec-0` 確定事項12）。加えて jsdom は `DragEvent`・`DataTransfer`・`setPointerCapture`・
`elementFromPoint`・`scrollIntoView` を持たない。**だから確定事項33 で挿入位置の計算を
純粋関数へ出す。**

| ファイル | 検証する内容 |
|---|---|
| `test/page-plan.test.js` | plan の初期値／並べ替え（1枚・複数枚・先頭へ・末尾へ・動かない場合）／回転の正規化（−90 で 270 になる・4回で元に戻る）／削除（最後の1枚を守る・削除後の選択）／dirty の判定（3回回して元に戻したら dirty でない）／undo/redo（上限50・分岐で先を捨てる）／選択の集合演算（Ctrl・Shift・Ctrl+Shift・起点の移動）／`dropIndex()`（列の中心の左右・行またぎ・端） |
| `test/page-grid.test.js` | 列数の決定（180/240/300/340/420px）／`layoutThumbnails` の `left` と `top`（1列のときは従来と同値）／ドラッグの閾値5px／Esc での取り消し／選択の見た目のクラス |
| `test/page-edit.test.js` | 写像（表示番号→元ページ）／回転で `sizes` の幅高さが入れ替わること／`pageCount` が追従すること／編集で `find` が捨てられること／ステータスバーの更新／タブごとに plan と履歴が分かれること／モードを切り替えても並びが残ること／キーの割り当て（Ctrl+Z / Ctrl+Y / Delete / Ctrl+A / Esc の優先順位）／dirty の表示と、閉じるときの確認が dirty のときだけ出ること |
| `test/shell.test.js` | `<script>` に新しいものが入ること／`#side-actions` が `button` 5つになること／`insert`・`undo`・`redo` のアイコンが描かれること |
| `test/dist-files.test.js` | 既存のまま。新しいレンダラーが `build.files` に載ることを自動で見張る |
| 既存 307件 | すべて緑のまま |

`test/harness.js` を広げる（`getPage()` スタブに `rotate` を持たせる、`getViewport({rotation})` を
効かせる、ポインタイベントの発火ヘルパー）。

---

## 完了の判定

すべて 2026-09-01 に Windows 11 実機で確認した。

- [x] `npm test` が緑（307 → **467件**。目標の400件以上を満たす）
- [x] `SIGK_SMOKE_PAGES=<操作列>` で、plan の並び・回転・選択・dirty・履歴の深さが出る
- [x] 同じ起動確認で、**ページビューの並びとサムネイルの並びが一致する**ことを数値で確かめた（`ratiosMatch: true`）
- [x] 回転したページが `SIGK_SMOKE_PRINT` の画像で**縦横が入れ替わる**（回転あり 1754×1240 ／ 回転なし 1240×1754）
- [x] **ドラッグ並べ替えを `Input.dispatchMouseEvent` で再現できた。**目視手順へ落とす必要は無くなった（下記）
- [x] `SIGK_SMOKE_TEXT` `SIGK_SMOKE_DROP` `SIGK_SMOKE_TABS` `SIGK_SMOKE_FIND` `SIGK_SMOKE_PRINT` が引き続き通る
- [x] `npm run dist` で NSIS インストーラーが生成され（108MB）、**生成物が起動する**
- [x] `screenshots/phase1-pages.png` を残す

### ドラッグ並べ替えの起動確認（未検証だった項目の決着）

起草時は「`Input.dispatchMouseEvent` で再現できるかは未検証。駄目なら目視手順へ落とす」と
していた。**再現できた。**

```
SIGK_SMOKE=1 SIGK_SMOKE_PDF=<pdf> SIGK_SMOKE_PAGES=<操作列> SIGK_SMOKE_DRAG=0-3 npx electron .
```

送るのは `mousePressed` / `mouseMoved` ×4 / `mouseReleased` だが、Chromium が互換のため
pointer 系も一緒に発火するので、ポインタ方式の実装（確定事項30）にそのまま届く。
mouseMoved を刻んで送るのは、1回で飛ばすと掴む判定と落とす判定が同じフレームに来るためである。

これで **`spec-1-4` の Ctrl+P と違い、塊④ には人の目でしか確かめられない項目が無い。**

### 実測（2026-09-01・Windows 11 実機）

`test/fixtures/huge-pages.pdf`（A4・1,000ページ・252KB）と `many-pages.pdf`（40ページ）で測った。
1,000ページの fixture は `SIGK_FIXTURES_HUGE=1 npm run fixtures` のときだけ作る
（毎回作ると `pretest` が3秒延びるため）。

| 測ったもの | 実測値 | 意味 |
|---|---|---|
| plan の作り直し（`movePages`） | 1,000ページで **0.3ms** | 確定事項1 の配列操作は、現実のページ数で問題にならない |
| 並べ替え1回から画面が揃うまで（`applyPlan`） | 1,000ページ **24.9ms** ／ 40ページ **6.2ms** | 枠の作り直しとレイアウトを含む。体感に響かない |
| 1,000ページ全部を回す | **47.7ms**（`undo` は 29.3ms） | 全選択して90度回す最悪の場合でも 50ms 以内 |
| 40ページでの各操作 | 回転 **6.9ms** ／ 並べ替え **7.4ms** ／ 削除 **6.8ms** ／ undo **6.7ms** | — |
| 3列のサムネイル | 紙 **111px**（パネル420px）、同時 **33枚**、JS ヒープ **8MB** | 上限 72枚（確定事項26）には**届かない**。届かせる必要も無い |
| 2列のサムネイル | 紙 **87px**（パネル240px）／ **127px**（パネル320px） | 確定事項23 の表より 8px 小さい（下記の訂正1） |
| ドラッグ中のフレーム落ち | 定量化できず | `Input.dispatchMouseEvent` で刻んで送る限り、取りこぼしも遅延も観測されなかった。自動スクロールと縦棒の描き直しが重い兆候は無い |

### 実測で直した前提（確定事項23・26 の訂正）

| # | 起草時の前提 | 実測 | 直し方 |
|---|---|---|---|
| 1 | 確定事項23 の「列数 = floor((内容幅 + 10) / 110)」。実測表は 240px→2列（紙95px） | **1列**になった。実測表が**縦スクロールバー（約16px）を勘定に入れていなかった**ため。実機では幅240pxのパネルの内容幅が 220px ではなく **204px** | 除数を **110 → 100** にした。列数は表（180→1／240→2／300→2／340→3／420→3）と**すべて一致**し、紙幅だけが小さくなる。差は列数で違い、**1列 −16px（150→134）／2列 −8px（95→87・125→117）／3列 −6px（90→84・117→111）**である |
| 2 | 確定事項26 の「同時に持つ上限 = 24 × 列数」 | 3列で **17枚**どまり。上限に遠く届かず、スクロールのたびに描き直しが起きていた。先読み（`THUMB_AHEAD=4`）が**枚数**で数えるため、3列では 1.3 行ぶんにしかならない | 先読みも列数倍にした（`thumbAhead()`）。3列で **33枚**になった。上限を列数倍にした以上、先読みも揃えないと上限が意味を持たない |

### 実装で分かったこと

| 見つけたこと | 直し方 |
|---|---|
| **起動確認が終わらなくなった。**編集して未保存になった状態で窓を閉じると、確認のダイアログ（確定事項57）が出たまま誰も押さず、`SIGK_SMOKE` が永久に終わらない | `SIGK_SMOKE` のときは確認を飛ばして閉じる。実運用の経路（未保存があれば聞く）は変えない |
| jsdom は `PointerEvent` を持つが `setPointerCapture` と `elementFromPoint` は**持たない** | どちらにも頼らない作りにした。確定事項31・33 が「挿入位置の計算を純粋関数へ出す」としていた判断は正しかった |
| jsdom の中で作られた配列は Node の配列と別のプロトタイプを持つ。`assert.deepEqual` が「same structure but not reference-equal」で落ちる | テスト側で `[...値]` を挟む（塊①からの流儀と同じ） |
| **回転したページがあると、ページビューに横スクロールバーが出る。**`layoutPages` は「いちばん広いページに合わせた器」を作り、「幅に合わせる」は現在ページを基準に倍率を決めるためである（`spec-1-1`） | 直していない。仕様どおりの帰結で、**既知の限界**として残す。気になるようなら「幅に合わせる」を最も広いページ基準にする |
| `page-plan.js` が 328 行になった | undo/redo を `page-history.js`（84行）へ出し、`page-plan.js` は 261 行になった。`page-grid.js`（**463行**）は割っていない。選択とドラッグは同じ状態を読み書きしており、分けると状態が2ファイルに散る。最終的な行数は `viewer.js` 601行・`thumbnails.js` 446行・`print.js` 388行なので、突出してもいない |

---

## 塊⑤へ申し送ること

塊④ の調査で分かったが、**保存を作るときに効く**もの。忘れないようここに残す。

| 事項 | 申し送る内容 |
|---|---|
| `save()` のオプション | **`{ updateFieldAppearances: false, updateMetadata: false, addDefaultPage: false }` を推す。**① 既定 `true` のままだと、**日本語の値が入ったフォーム PDF はページを1枚消しただけで保存が失敗する**（実測）② Producer を pdf-lib に書き換えない ③ 白紙 A4 を勝手に生やさない。塊⑤ の仕様として明文化する |
| 削除でサイズが減らない | 1,000ページ・195.7MB から 500ページ削除しても 195.7MB のまま。`copyPages` で作り直せば減るが、しおり・AcroForm・名前付き宛先が必ず消える。**「サイズは減りません」を既知の限界として書き残す**のが第1版の推し |
| `PageLabels` | 並べ替え・削除で追従しない。挿入では作られない。**他のビューアで開いたときだけ見える破損**。直すか既知の限界にするかを塊⑤ で決める |
| 進捗と中止 | `save()` に進捗も中止も無い。1,000ページ・195.7MB で save 1,634ms。進捗は**読み込み／適用／書き出しの3段**でしか出せない |
| メモリ | pdf-lib 側だけで最大 678MB（1,000ページ・195.7MB を2本扱ったとき）。非機能要件 1.5GB に対し、pdf.js が同じファイルを抱えている分は別勘定。**合算の見積もりは未取得** |
| 保存はパスから読み直す | pdf.js が開いた時点でバイト列はワーカーへ転送され、レンダラーに残らない。**開いてから保存するまでに元ファイルが外部で書き換えられた／移動された場合の扱いが未定** |
| pdf-lib で開けない文書 | 内容が欠損した PDF は `load` が素の `TypeError` になり、警告は `console.warn` へ直接出て `log:error` に載らない。**「閲覧はできるが保存だけできない文書」の見せ方が未定** |
| 挿入（F-02-5）の実装上の壁 | ① `file-io.js` の `readPdf` は拡張子 `.pdf` 以外を弾くので、画像を入れるなら**メイン側に別の読み込み口**が要る ② pdf-lib が埋め込めるビットマップは **PNG と JPEG だけ**。GIF・BMP は `message` すら持たない値を投げるので、**形式の判定と文言はアプリ側で持つ** ③ `copyPages` した内部リンクは壊れ、余分なページオブジェクトが混入する |

> **2026-09-01 訂正（塊⑤ の事前調査で判明）。**上の表の1行目（`save()` のオプション）は `updateMetadata` を
> `save()` のオプションとして書いているが、**これは `load()` のオプションである。**
> `PDFDocument.load(bytes, { updateMetadata: false })` と書かないと Producer は
> 読み込んだ時点で pdf-lib に書き換わる。また `updateFieldAppearances` による保存の失敗は、
> **その文書で `getForm()` を呼んだときにだけ**起きる（`save()` の中は
> `formCache.getValue()` を見ており、触っていない文書ではキャッシュが空のため何もしない）。
> 正しい形と根拠は `docs/spec-1-6-save.md`「事前調査 B」にある。
>
> **同じく訂正が2件。**
> ① 上の表の「`copyPages` での複製 → しおりと AcroForm が必ず消える」は、
> **新規文書を作って全ページを copy し直す方式に限った話である。**開いている
> `PDFDocument` へ `copyPages` ＋ `insertPage` する分には、その文書のしおり・AcroForm・
> 内部リンクは保たれた（実測）。ただし**差し込まれた側**の `/Widget` 注釈だけは
> ページに付いたまま運ばれ、機能しない入力欄の抜け殻になる。
> ② 「GIF・BMP は message すら持たない値を投げる」は **`embedPng` についてのみ正しい。**
> `embedPng` はプリミティブの文字列 `"The input is not a PNG file!"` を投げるが、
> `embedJpg` は本物の `Error`（`"SOI not found in JPEG"`）を投げる。同じライブラリの中で
> 型が揃っていない。詳細は `docs/spec-1-6-save.md`「事前調査 H」。

---

## 未確定のまま残すもの

| 項目 | 扱い |
|---|---|
| 矩形選択（確定事項20） | 入れない（決定2）。要望が出てから、サムネイルの無い空白部分からのドラッグに限って足す |
| 見開き表示（F-01-1 後半） | `docs/07` 第6章のまま **Phase 1 完了時に決める**。塊④ の作りとは衝突しない（グリッドの列数を可変にするので、ページビュー側の2列化は `layoutPages` の話に閉じる） |
| ~~ドラッグ並べ替えの起動確認~~ | **解決済み**（2026-09-01）。`Input.dispatchMouseEvent` で再現できた。`SIGK_SMOKE_DRAG` として起動確認に入れた |
| サムネイル上限 `24 × 列数`（確定事項26） | 実測では 3列で 33枚どまりで、上限には届かない。**上限そのものは効いていない**が、メモリは 8MB に収まっており下げる理由も無い。常用して見直す |
| undo の履歴50世代（確定事項9） | 実測に基づかない初期値のまま。常用して見直す |
| 回転したページがあるとページビューに横スクロールバーが出る | **既知の限界**（上記「実装で分かったこと」）。「幅に合わせる」を最も広いページ基準にすれば直るが、回転していない文書での挙動が変わるため、常用して不満が出てから決める |
| 紙の上のホバーボタンの操作性 | 紙が 87〜127px と小さいため、22px のボタンを3つ載せると紙の3割ほどを覆う。常用して邪魔なら、ホバーの遅延を入れるか回転だけに絞る |

---

## ユーザーの確定（2026-09-01）

起草時に残した論点4件は、同日ユーザーが決定した。いずれも起草時の推し案のとおりである。

### 決定1: 抽出（F-02-4）と挿入（F-02-5）は塊⑤へ送る

`docs/05` は両方を塊④ に割り当てていたが、**どちらもファイルを書く操作**であり、
書き出し経路（ワーカー・`showSaveDialog`・進捗・中止）は塊⑤ の担当と定めてある。
いま `worker/` も保存ダイアログも無く、並びを組むだけでは何も起きない。挿入はさらに、
メイン側の画像読み込み口（`file-io.js` は拡張子 `.pdf` 以外を弾く）・PNG/JPEG の判定
（pdf-lib はこの2つしか埋め込めない）・2本目の文書の描画を引き連れる。

**塊④ ＝ 画面の上で並びを組む／塊⑤ ＝ それをファイルへ書く**で割る。塊③ を ③-a／③-b に
割ったのと同じ理由（量と依存の向き）である。`docs/05` の要件の割り当てを書き換える。

### 決定2: 矩形選択（F-02-6 の後半）は第1版に入れない

`docs/04` 4-2 に記述が無く、サイドパネル上の左ドラッグは並べ替え（F-02-1）に割り当て済みで
競合する。連続範囲は Shift クリック、飛び飛びは Ctrl クリックで実用上は足りる。
**既知の限界として書き残し**、要望が出てから「サムネイルの無い空白部分からのドラッグに限る」
形で足す。

### 決定3: 未保存の表示と、閉じるときの2択の確認を塊④ に入れる

`docs/07` の積み残しは未保存の警告（F-06-5）を塊⑤ 送りにしていたが、**dirty になる経路は
塊④ で初めて生まれる**。何も入れないと、編集したタブを閉じたときに確認も出ず編集が消える。
表示（タブの点・ステータスバーの「変更あり」）と2択の確認までを前倒しし、「保存」を含む
3択は塊⑤ で完成させる。詳細は確定事項56〜58。

### 決定4: `docs/02` 2-3 の `ops` を「結果の並び」へ改訂する

確定事項1〜3 のとおり。ワーカーへ渡す形を `{ source, pages, ops, target }` とし、
`pages` がページの並び（並べ替え・回転・削除・抽出・挿入）、`ops` がページに足すもの
（注釈・透かし）を担う住み分けを `docs/02` に明記する。
「undo/redo が操作の列の巻き戻しとして自然に書ける」という記述は**事実に反する**ので削り、
「plan のスナップショット列で持つ」に置き換える。
