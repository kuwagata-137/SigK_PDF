# 開発計画の決定事項

決定日: 2026-08-29
関連: `docs/02_アーキテクチャ設計.md` 第9章 ／ `docs/04_UI設計.md` 第10章 ／ `docs/05_開発ロードマップ.md` ／ `docs/spec-0-foundation.md`

Phase 0 の実装に入る前に、開発の進め方と技術的前提を確定させた記録である。本書の決定は
各ドキュメントへ反映する。反映が済んだ項目は「反映先」の欄に記す。

---

## 1. 調査で判明した事実

決定の根拠となる実測と調査。いずれも 2026-08-29 に本リポジトリの開発環境で確認した。

> **2026-08-31 追記。** 開発環境を Linux コンテナから Windows 11 実機へ移した。
> 下の表のうち #1・#2 は当時の環境に固有の観測であり、現在は当てはまらない。#5 は
> **当時から誤りだった**ので訂正する。読み替えは第2章の決定16・17、および `spec-0`
> 「検証をどこで回すか」を参照。#3・#4 は Chromium の仕様に関する観測であり、
> 環境が変わっても有効である。

| # | 確認したこと | 結果 |
|---|---|---|
| 1 | `npm install` | 不可。`registry.npmjs.org` が 403（ネットワーク許可リスト外）<br>**2026-08-31: 可。**Windows 実機で ping 204ms、Node 24.12.0 ／ npm 11.19.1 |
| 2 | Chromium の有無 | あり（`/opt/pw-browsers/chromium-1194/`）。`file://` で HTML を開き DOM を取得できる<br>**2026-08-31: この経路は使わない。**実機で Electron を直接起動して確認できるため |
| 3 | `<script>` を `file://` から読む | 動く |
| 4 | `<script type="module">` を `file://` から読む | **動かない**。`--allow-file-access-from-files` を付けたときだけ動いた |
| 5 | CheckListMaker の参照可否 | ~~可。`test/harness.js`・`errorlog.js`・CI 2本とも実在し、移植できる~~<br>**2026-08-31 訂正: 不可。**別リポジトリであり、リポジトリ境界の運用ルールにより参照しない。この作業環境にクローンも存在しない。**harness・errorlog・CI 2本はいずれも新規に書き起こした** |
| 6 | CheckListMaker のコード署名方針 | `docs/spec-3.5-A-code-signing.md` に 2026-07-14 付けユーザー決定「一切お金をかけない方針のため証明書の購入は見送り、無署名で配布を継続」 |
| 7 | リモートブランチの状態 | PR #1 の `claude/pdf-app-development-3c5yn0` が main と同 SHA のまま残存 |
| 8 | 日本語フォント（開発環境） | IPAGothic があり、モックの描画確認はできる。ただし実機の Windows は Yu Gothic UI であるため、字形は本番と異なる |
| 9 | ツールレールの実寸 | ラベルを出しても **72px** で収まる。`docs/04` の「56px では足りない」は正しいが、必要幅は 72px |
| 10 | サイドパネルを畳んだときの挙動 | 空いた 240px は全部 `--canvas` に消え、**ページの幅は変わらない**。畳んでも読める面積が増えない |

### 事実4が意味すること

`file://` のページは不透明なオリジンを持つため、モジュールスクリプトが CORS で弾かれる。
Electron も同じ Chromium を使うため、結論は変わらない。したがって
`docs/02` 第9章の論点1（pdf.js 4系の ES Modules 配布が `file://` で読めるか）は
**読めない**が答えである。Phase 1 の先頭で確認するとしていたが、ここで決着した。

---

## 2. 決定事項

| # | 論点 | 決定 | 反映先 |
|---|------|------|--------|
| 1 | 実装の動作確認をどこで回すか | ~~CI を機械的な判定の正とする。この環境の Chromium で DOM とスクリーンショットを確認してから push。実機での確認は各フェーズ末に1回~~<br>**2026-08-31 改訂: 手元（Windows 実機）を第一の検証の場とする。**`npm install`・`npm test`・`npm start`・`npm run dist` を push 前にすべて通す。CI はクリーンな環境での再現性の担保とインストーラーの Artifact 化を担う。実機確認が常時できるようになったため、各フェーズ末に1回という制約は外れた | `docs/05`、`spec-0` |
| 2 | レンダラーの配信方式 | `main.js` の `protocol.handle` で `app://` スキームを登録し、レンダラーをそこから配信する。`file://` は使わない | `docs/02` 第6章、`spec-0` 確定事項4・5 |
| 3 | pdf.js のバージョン | 4系以降（保守が継続しているもの）。`app://` 上では ESM が普通に動くため3系へ落とす必要はない。**2026-08-31 時点の採用は `^6`（6.3.289）** | `docs/02` 1-3、`spec-0` 確定事項10 |
| 4 | レンダラー自身のモジュール形式 | IIFE ＋ `window.SigK` を維持する。ESM を使うのは pdf.js の読み込み1点に限る | `spec-0` 確定事項1・2（変更なし） |
| 5 | 常用を始める時点 | Phase 1 完了時。インストーラーを実機に入れて日常的に使い始め、Phase 2 以降の優先度は実地で出た不満を根拠に見直す | `docs/05` |
| 6 | Phase 1 への前倒し | ①上書き保存時の `.bak` 作成（元 6-2）②コマンドライン引数の解釈＋単一インスタンス（元 5-1・5-2） | `docs/05` |
| 7 | PR の単位 | 意味のある塊ごと。Phase 1 は5本に分ける（後述3章） | `docs/05` |
| 8 | コード署名 | 購入しない。無署名で配布する。CheckListMaker `spec-3.5-A` の決定をそのまま流用し、無償の緩和策（README に SmartScreen の手順、Releases に SHA-256 を添付、ソース公開）も引き継ぐ | `docs/02` 第9章、`README.md` |
| 9 | 自動更新 | 入れない。electron-updater を採用せず、アプリは一切通信しない。更新は GitHub Releases または Actions の Artifact からインストーラーを落として上書き実行する | `docs/02` 第9章 |
| 10 | `test/fixtures/` の用意 | `test/fixtures/build.js` を置き、pdf-lib で生成する。CI のテスト前に実行し、PDF バイナリはリポジトリに置かない（`.gitignore`） | `docs/02` 第8章 |
| 11 | UI の確定手順 | Phase 0 の実装前にモックを作り、Chromium のスクリーンショットで提示して `docs/04` 第10章の未確定事項2・3を潰す。モックは使い捨てにせず Phase 0 の 0-4 の成果物に育てる | `docs/04` 第10章（**実施済み**。結果は決定13〜15） |
| 12 | ブランチ運用 | セッション側の指示は `main` での開発を指定しているが、`.claude/CLAUDE.md` 領域3（`main` へ直接 push しない）を優先し、作業ブランチで進める | 本書 |
| 13 | サイドパネルの既定 | 開いた状態で起動する。畳むのはユーザーが選べる | `docs/04` 第3章・第10章 |
| 14 | ツールレールの表示 | アイコン＋文字。レール幅は 56px から **72px** へ広げる | `docs/04` 第3章・第10章 |
| 15 | ダークモード | **搭載しない。**ライト1本で作る。CheckListMaker とはここだけ仕様が異なるが、それで構わないというユーザー判断（2026-08-29） | `docs/04` 1-2・第2章、`spec-0` 確定事項6、`docs/05` 0-4・6-2 |
| 16 | 依存バージョンの更新 | Electron `^31`→**`^44`**、pdfjs-dist `^4`→**`^6`**、electron-builder `^24`→**`^26`**（ユーザー判断 2026-08-31）。Windows 実機で検証できるようになったため、古い版から始めて後で上げるより手戻りが少ない | `docs/02` 第7章、`spec-0` 確定事項10・15 |
| 17 | 開発環境を Windows 11 実機へ移行 | 検証方針を決定1のとおり改める。Linux コンテナを前提にした記述（`docs/02` 8-1、`docs/05` の三段構え、`spec-0` の環境節、本書第1章 #1・#2・#5、第4章）をすべて訂正する（2026-08-31） | `docs/02`、`docs/05`、`spec-0`、本書 |
| 18 | 同梱ライブラリの依存区分 | pdfjs-dist・pdf-lib・fontkit は `devDependencies` に置く。実行時に `node_modules` から `require` せず `vendor/` を読むため。`dependencies` に置くと electron-builder が `node_modules` を丸ごと同梱し、配布物が約19MB 膨らむ（実測 126.3MB → 107.3MB）（2026-08-31） | `docs/02` 第7章、`spec-0` 確定事項17 |

### 決定9の根拠

README の「すべての処理をローカルで行い、ファイルを外部へ送らない」「ネットワーク接続は不要」は、
機能を絞った本アプリの数少ない差別化点である。加えて CheckListMaker `spec-3.5-A` が指摘するとおり、
無署名のアプリに自動更新を載せると更新ファイルの真正性を検証する手段がない。**無署名と自動更新の
組み合わせが最も危険**であるため、署名しない以上は自動更新も入れない。

### 決定15 の実装上の含意

ダークモードを持たないため、次の2点を実装時に入れる。忘れると気づきにくい。

- `<meta name="color-scheme" content="light">` を明示する。これが無いと、OS がダーク設定の環境でフォーム部品・スクロールバー・既定の配色が暗転し、ライト固定の画面と噛み合わない。
- ウィンドウ枠は `frame: true`（OS 標準）であるため、OS がダークのとき**枠だけダーク・中身は白**という組み合わせになる。これは仕様として受け入れる。独自のタイトルバーは描かない（`spec-0` 確定事項11）。

---

## 3. Phase 1 の分割

15項目（元の13項目＋前倒し2件）を5本の PR に分ける。①を先頭に置くのは、`app://` と
pdf.js の統合が本当に動くかを最初に確かめ、駄目なら決定2・3を早期に見直せるようにするためである。

| # | 塊 | 含む項目 |
|---|---|---|
| ① | 表示の最小 | `app://` 配信、pdf.js 統合、連続スクロール、ズーム |
| ② | 入出力とタブ | ダイアログ、ドラッグ＆ドロップ、タブ、最近使ったファイル、文書情報 |
| ③ | 読むための道具 | サムネイル、検索・選択・コピー、印刷 |
| ④ | ページ編集 | ページモード、`ops` の組み立て、undo/redo |
| ⑤ | 保存の完成 | ワーカーと進捗・中止、保存、`.bak`、未保存警告、パスワード付き PDF、引数と単一インスタンス |

---

## 4. テストの二層構造

**2026-08-31 改訂。** かつては「この環境には pdf-lib も pdf.js も無いため、PDF バイト列を
触るテストはすべて CI 送りになる」と書いていた。**現在は両層とも手元で回る。**

二層に分ける方針そのものは維持するが、理由が変わった。目的は、依存を要さない層を厚く
保ってテストの起動を速くし、依存の更新で壊れる範囲を局所化することにある。

| 層 | 対象 |
|---|---|
| 依存なしで回る層 | `ops` の組み立て・正規化、undo/redo のスタック操作、設定の読み書き、セキュリティ判定、パス解決、`planVendorCopy()` |
| 依存を要する層 | ワーカーの `op-*.js`、fixture を読んだ検証、jsdom を要する画面テスト |

`docs/02` 2-3 が既に「組み立て」と「適用」を分けているが、テスト可能性の観点からも
この境界を意識して引く。

---

## 5. 調査で解消した懸念

| 懸念 | 結論 |
|---|---|
| `PENDING_WINDOW_MS` の実測が Phase 1 に割り込むのではないか | 割り込まない。引数の集約が要るのは右クリックメニューが N ファイルに対し N プロセスを起動する `merge`・`split`・`toPdf` の場合であり、いずれも Phase 2 以降。Phase 1 の `open` は、パスが来るたびにタブを1枚足せば済む。実測は Phase 5 に残す |
| Phase 0 の harness と errorlog を書き起こす必要があるか | ~~不要。CheckListMaker から複製できる~~<br>**2026-08-31 訂正: 必要。**参照できないため新規に書き起こした。CheckListMaker と同じ「形」（`node --test` ＋ jsdom、JSONL のエラーログ）は守っている |
| CheckListMaker の CI をそのまま持ち込めるか | ~~`test.yml` は可~~<br>**2026-08-31 訂正: 持ち込まない。**2本とも新規に書いた。ブランチ名の決め打ちは避け、`push: branches: ['**']` ＋ `pull_request` ＋ `workflow_dispatch` で起動する |

---

## 6. 積み残し

| 項目 | 扱い |
|---|---|
| ~~`docs/04` 第10章 未確定事項2・3~~ | **解決済み**（決定13・14） |
| `docs/04` 第10章 未確定事項4（注釈プロパティの位置） | Phase 4 の着手時に決める |
| 暗号化 PDF の fixture 生成手段 | pdf-lib は暗号化した書き出しに対応しないため、選定済みの依存だけでは作れない。塊⑤の着手時に詰める（qpdf を devDependency に足すか、暗号化だけ手動確認に留めるか） |
| Phase 2 以降の機能の優先度 | 決定5 に従い、Phase 1 の常用で出た不満を根拠に見直す |
| 異常系（壊れた PDF・巨大な PDF・暗号化 PDF）の確認 | Phase 1 の完了条件には含めない。ロードマップ 6-3 のまま残す |
| ~~リモートブランチが消せない件~~ | **解決済み**。ユーザーが「Automatically delete head branches」を有効にした（2026-08-29）。以降マージのたびに自動削除される |

---

## 7. 次の手順

1. ~~UI モックを作り、Chromium のスクリーンショットで提示する~~ — 済
2. ~~`docs/04` 第10章の未確定事項2・3を合意する~~ — 済（決定13・14。併せて決定15）
3. ~~本書の決定を `docs/02`・`docs/04`・`docs/05`・`docs/spec-0-foundation.md` へ反映する~~ — 済
4. ~~Phase 0 を実装する~~ — 済（2026-08-31。`claude/phase-0-foundation` ブランチ。完了判定6項目すべて充足）
5. 作業ブランチを push し、**ユーザーの指示を受けてから** PR を1本立てる（領域3）
6. Phase 1 の塊①（`app://` からの配信・pdf.js の統合・連続スクロール・ズーム）の仕様書 `docs/spec-1-1-viewer.md` を書く

`README.md` は確認したが書き換えが要る箇所は無かった。技術構成の記述（pdfjs-dist・ビルドステップなし）は今回の決定と矛盾せず、「ネットワーク接続は不要」も決定9 と一致する。
CI のワークフロー2本は、`package.json` が無い状態で置くと CI が赤になるため、**Phase 0 の実装 PR に含める**。
