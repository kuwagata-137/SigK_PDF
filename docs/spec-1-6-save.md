# 仕様書: Phase 1 塊⑤ 保存の完成

起草日: 2026-09-01
ステータス: **確定**（2026-09-01。論点7件はユーザーが決定。末尾「ユーザーの確定」を参照）
関連: `docs/05_開発ロードマップ.md`（塊の割り当て）／`docs/02_アーキテクチャ設計.md` 2-3・第5章／
`docs/04_UI設計.md` 第6章・第7章／`docs/spec-1-5-page-edit.md`「塊⑤へ申し送ること」

---

## 目的

塊④ で「画面の上でページの並びを組む」までができた。組んだ並びを**ファイルへ書く**手段が
無いため、編集しても何も残らない。塊⑤ でこれを閉じ、Phase 1 を完了させる
（＝常用を始める。`docs/07` 決定5）。

---

## 含めるもの / 含めないもの

`docs/05` が塊⑤ に割り当てているのは次の7項目である。

| 項目 | 要件 |
|---|---|
| ワーカー（`utilityProcess`）と進捗・中止 | 非機能 |
| 上書き保存・名前を付けて保存 | F-06-2 |
| 未保存の警告（「保存」を含む3択） | F-06-5 の残り |
| `.bak` の作成 | 前倒し（`docs/07` 決定6） |
| 抽出・挿入 | F-02-4、F-02-5 |
| パスワード付き PDF の読み込み | F-06-6 |
| コマンドライン引数＋単一インスタンス | F-07-1、前倒し |

含めないもの: 結合・分割・変換・注釈・透かし（Phase 2 以降）、暗号化しての**書き出し**
（第2版。F-06-6 は読み込みのみ）、Phase 1 末の非機能要件の実測（塊⑤ の完了後に別途行う）。

---

## 事前調査（2026-09-01・Windows 11 実機で実測）

推測で仕様を書かないため、先に実物で測った。塊③-b・塊④ と同じ手順である。
`docs/spec-1-5-page-edit.md`「塊⑤へ申し送ること」の7件が出発点になっている。

### A. ワーカー（`utilityProcess`）

**最大の未知**は「asar に固めたあとでもワーカーを起動できるか」だった。ミニ asar を
`@electron/asar` で作り、実際に `utilityProcess.fork` して確かめた。

| 調べたこと | 分かったこと |
|---|---|
| asar の中のスクリプトを fork できるか | **できる。**`app.asar/worker.js` を指して `fork` が成功し、`__dirname` は `.../app.asar` になる |
| asar の中から `pdf-lib.min.js` を `require` できるか | **できる。**`require(path.join(__dirname, 'pdf-lib.min.js'))` が通り、`PDFDocument` が取れた |
| 親子のメッセージ | 双方向とも動く。子は `process.parentPort.postMessage(...)`／受けは `e.data`。親は `child.on('message', m => ...)` で**素の値**が届く（`e.data` ではない） |
| 中止（`child.kill()`） | 即座に効く。**元ファイルは無傷**で、書きかけの**一時ファイルだけが残る**（3MB のまま残った）。後始末は呼び出し側の仕事 |

**3段の時間**（1,000ページの fixture・asar のワーカー内）:

| 段 | 時間 |
|---|---|
| read（ディスクから読む） | 3ms |
| load（pdf-lib の解析） | 328ms |
| apply（全ページ逆順の並べ替え＋回転） | 58ms |
| save（バイト列の組み立て） | 680ms |
| 合計 | **1,096ms**・ワーカーの rss 82MB |

`docs/04` 第6章の「1秒を超えそうな処理は進捗と中止」に**ちょうど当たる**。
`docs/spec-1-5` の実測（195.7MB で save 1,634ms）と合わせて、**進捗は必須**である。
`save()` 自体に進捗の口が無いので、**read／load／apply／save／write の5段の切り替わりでしか
出せない**。段の中では進まない。

### B. pdf-lib のオプション（申し送りの訂正を含む）

| 調べたこと | 分かったこと |
|---|---|
| **`updateMetadata` の置き場所** | **`load` のオプションであって `save` のものではない。**`load` 既定（`true`）の時点で Producer が pdf-lib に書き換わる。`save({updateMetadata:false})` を渡しても**効かない**。**`docs/spec-1-5` の申し送りは置き場所を誤っていた**ので、ここで訂正する |
| `updateFieldAppearances` の実害 | **`getForm()` を呼んだときだけ**牙を剥く。`save()` の中は `form = this.formCache.getValue(); if (form) form.updateFieldAppearances();` で、**`getForm()` を一度も呼んでいない文書ではキャッシュが空**なので何も起きない。呼んだ場合は `/AP` を持たない和文の欄で `WinAnsi cannot encode "氏" (0x6c0f)` になる（実測で再現） |
| `addDefaultPage:false` | 全ページ削除して保存すると**0ページの PDF が出来てしまう**（既定 `true` なら白紙 A4 が1枚生える）。塊④ の「最後の1枚は消せない」で塞いである |
| 削除とサイズ | **減らない。**1,000ページを半分削除して `removePage` は 0.2MB のまま、`copyPages` で作り直すと 0.1MB。申し送りのとおり |
| 抽出・挿入の速さ | 抽出3枚 1.3ms、挿入1枚 11.7ms。**軽い** |
| 壊れた PDF | 空 → `No PDF header found`／ヘッダだけ → **例外なし**（0ページの文書として通る）／途中で切った → `Failed to parse invalid PDF obj` |

### C. Windows のファイル事情（上書き保存の実現方法）

| 状況 | 直接 `writeFile` | 一時ファイル → `rename` |
|---|---|---|
| 他のプログラムが**読み取りで開いている** | **成功** | **失敗（EPERM）** |
| 読み取り専用属性 | 失敗（EPERM） | 失敗（EPERM） |
| 何も掴んでいない | 成功 | 成功（上書きされる） |

`os.tmpdir()` からの `rename` は同一ドライブなら通ったが、**ユーザーのファイルは別ドライブに
あり得る**。一時ファイルは**保存先と同じフォルダー**に置く。

外部での書き換えは `size` と `mtimeMs` の比較で検出できる（実測で 47ms の差が出た）。

### D. パスワード付き PDF（pdf.js 側）

暗号化した fixture は**依存を増やさずに作れた**。ISO 32000-1 の Algorithm 2／3／4
（RC4 40bit・`/V 1 /R 2`）を Node の `crypto`（MD5）＋自前の RC4 で書き、約 900 バイトの
PDF を生成した。OpenSSL 3 は RC4 を既定で持たないため RC4 だけ自分で書いている。

| 調べたこと | 分かったこと |
|---|---|
| パスワードなしで開く | `PasswordException`（`name` がそのまま `"PasswordException"`、`code` 1、`message` `"No password given"`） |
| `PasswordResponses` | `{ NEED_PASSWORD: 1, INCORRECT_PASSWORD: 2 }` |
| 聞き方 | `loadingTask.onPassword = (updatePassword, code) => {...}` を**`getDocument()` の直後に代入する**。`updatePassword('user1')` で続行 |
| 間違えたとき | 同じ `onPassword` がもう一度呼ばれ、`code` が 1 → 2 と流れる。**何度でも聞き直せる** |
| 取りやめ | `updatePassword(new Error(...))` で `promise` が reject する |
| オーナーパスワード | ユーザーパスワードと同様に開ける |

### E. 【重大】暗号化 PDF は保存すると壊れる

pdf-lib は暗号化した文書を `load(bytes, { ignoreEncryption: true })` で読めるが、
**復号はしない**。この状態で `save()` すると次のようになった。

- 保存は**成功してしまう**（887 バイトが出力される）
- 出力に `/Encrypt` と `/Filter /Standard` が**そのまま残る**
- 内容ストリームは**暗号化されたまま**（平文の `Encrypted fixture` は現れない）
- 出来た PDF を pdf.js で開くと**正しいパスワードでも開けない**（8秒待っても解決しない）

つまり `ignoreEncryption` を使った保存は**黙ってファイルを壊す**。
**暗号化 PDF は「開けるが保存できない文書」として扱う**（確定事項12）。

### F. 「閲覧はできるが保存だけできない文書」は本当にあるか

`docs/spec-1-5` の申し送りは「pdf.js で開けて pdf-lib で開けない文書がある」としていた。
どういう検体を用意すればよいかを確かめるため、`three-pages.pdf` を5通りに壊して
両方に読ませた。

| 壊し方 | pdf-lib | pdf.js |
|---|---|---|
| 末尾を切る（400B） | `Failed to parse PDF document` | `InvalidPDFException: Invalid PDF structure.` |
| 末尾を切る（半分） | `Failed to parse PDF document` | `InvalidPDFException: Invalid PDF structure.` |
| `startxref` の値を壊す | `TypeError: Cannot read properties of undefined` | `InvalidPDFException: Invalid Root reference.` |
| xref 表ごと消す | `TypeError: Cannot read properties of undefined` | `InvalidPDFException: Invalid Root reference.` |
| オブジェクト参照を壊す | `TypeError: Cannot read properties of undefined` | `InvalidPDFException: Invalid Root reference.` |

**5通りとも両方が失敗した。**単純に壊した PDF では、pdf.js だけが開ける状態は作れない。
つまり**壊れた PDF はそもそもタブにならず、保存の経路に到達しない**。

申し送りが指していた素の `TypeError`（`Cannot read properties of undefined (reading 'Pages')`）は
再現できたので、**ワーカー側で例外の型を選ばずに握る**必要はある。ただしそれは
「開けた文書が保存だけできない」ための備えではなく、**予期しない入力に対する防具**である。

**実際に「閲覧できて保存できない」のは暗号化 PDF である**（実測E）。こちらは pdf.js が
パスワードを聞いて開き、pdf-lib は `load` を断る。確定事項11 と12 の重みが逆で、
**12 が主・11 が従**である。

---

## 塊⑤ は割らず、1本の PR で仕上げる

起草時は ⑤-a（保存の芯）と ⑤-b（抽出・挿入・パスワード・引数）の2本に割ることを推した。
量が塊④（テスト +160件）を超え、依存が片方向であるためである。

**ユーザーの決定は「1本のまま進める」**（2026-09-01）。したがって本書は塊⑤ の全7項目を
対象に確定させる。割らないことの帰結を2つ引き受ける。

- **PR は大きくなる。**新規モジュール5本・既存への追記7本を1本に載せる。代わりに
  Phase 1 の完了（＝常用開始。docs/07 決定5）が一度に来る。
- **⑤-b へ送るつもりだった論点も、いま確定させる。**抽出・挿入の対応形式、
  パスワード付き PDF の聞き方、引数の解釈がそれにあたる。

## 確定事項

### A. ワーカーと進捗・中止

| # | 論点 | 決定 |
|---|------|------|
| 1 | ワーカーの起動 | `task-runner.js`（メイン）が `utilityProcess.fork(worker/pdf-task.js)` する。asar の中から起動できることは実測済み。**タスク1本につきプロセス1つ**を立て、終わったら落とす。使い回さない（メモリを抱えたままにしないため） |
| 2 | ワーカーへ渡す形 | `docs/02` 2-3 のとおり `{ source, pages, ops, target }`。塊⑤ では `ops` は常に空配列（注釈・透かしは Phase 4） |
| 3 | ワーカーが読むもの | **パスだけを渡し、ワーカー自身がディスクから読む**（`docs/02` 2-2）。バイト列は IPC に載せない |
| 4 | pdf-lib の読み込み | ワーカーが `require(path.join(__dirname, '../vendor/pdf-lib.min.js'))` する。`node_modules` は配布物に無い（`docs/07` 決定18） |
| 5 | 進捗の段 | **`read` → `load` → `apply` → `save` → `write` の5段。**段の切り替わりでのみ報告する。`save()` に進捗の口が無いため、段の中は進まない（実測B） |
| 6 | 進捗の見せ方 | ページビュー上端の帯（`renderer/view-banner.js` が既にある）を使い回す。モーダルにしない（`docs/04` 第6章）。文言は「保存しています（3/5）」 |
| 7 | 帯を出す条件 | **押した瞬間から出す。**小さい文書では一瞬で消える。「1秒を超えたら出す」にすると、出るか出ないかが文書によって変わり、中止ボタンを狙えない |
| 8 | 中止 | `child.kill()`。実測どおり元ファイルは無傷で、**一時ファイルが残るのでメイン側が消す**（確定事項16） |
| 9 | 二重起動の防止 | 同じタブで保存中は、保存・名前を付けて保存・閉じる・終了をすべて塞ぐ。タブをまたいだ同時保存は**許す**（別プロセスなので干渉しない） |
| 10 | 失敗の伝え方 | ワーカーは例外を握って `{ ok:false, error:'人が読める文言' }` で返す。`docs/02` 第5章の作法に合わせ、レンダラーで例外を投げない |

### B. 保存できない文書の扱い

| # | 論点 | 決定 |
|---|------|------|
| 11 | pdf-lib で開けない文書 | **防具として置く**（実測F）。単純に壊れた PDF は pdf.js も開けないため、**タブにならず保存の経路へ来ない**。それでも想定外の入力でワーカーが素の `TypeError` を投げることはあるので、例外の型を選ばずに握り、「この PDF は内容が壊れているため保存できません。」に翻訳する。素の文言は表に出さない |
| 12 | **暗号化 PDF** | **保存を断る**（実測E）。`ignoreEncryption` で保存するとファイルが壊れるため、**成功したように見せてはならない**。文言は「パスワードで保護された PDF は保存できません。」。閲覧・印刷は従来どおりできる |
| 13 | いつ判定するか | **保存を押したとき**（開いたときではない）。開く経路に pdf-lib の読み込みを足すと、開くのが 328ms（1,000ページ）遅くなるうえ、保存しない文書にも払わせることになる |
| 14 | 0ページの保存 | 起き得ない。塊④ が最後の1枚を消せないようにしてある（`spec-1-5` 確定事項41）。念のためワーカー側でも `pages.length === 0` を断る |

### C. 上書き保存の実現方法

| # | 論点 | 決定 |
|---|------|------|
| 15 | 書き方 | **保存先と同じフォルダーに一時ファイル（`<名前>.pdf.sigk-tmp`）を作り、`rename` で置き換える。**書きかけで元ファイルを壊さないため。一時ファイルを `os.tmpdir()` に置かないのは、別ドライブだと `rename` が使えないからである（実測C） |
| 16 | 一時ファイルの後始末 | 成否にかかわらず消す。中止・失敗で残ったものも消す。**残すと「一時ファイルは終了時に削除する」（非機能要件）に反する** |
| 17 | `rename` が EPERM のとき | 他のプログラムが元ファイルを開いていると `rename` だけが失敗する（実測C）。**黙って直接上書きに切り替えない。**「ファイルが他のプログラムで使われています。閉じてからもう一度お試しください。」と出して中止する。直接書き込みは成功してしまうが、途中で落ちたときに元ファイルが失われる |
| 18 | `.bak` | **上書き保存のときだけ作る。**`<名前>.pdf.bak` へ元ファイルを `copyFile` してから書き換える。既にある `.bak` は上書きする（世代は持たない）。名前を付けて保存では作らない（元ファイルを触らないため） |
| 19 | `.bak` の作成に失敗したら | **保存そのものを中止する。**退避できないまま上書きするのは、`.bak` を作る意味を失わせる |
| 20 | `.bak` の既定 | Phase 1 では**常に作る**。設定で選べるようにするのはロードマップ 6-2 のまま |
| 21 | 保存前の照合 | 開いたときの `size` と `mtimeMs` を覚えておき、保存の直前に照合する。食い違えば「このファイルは開いたあとで変更されています。上書きすると、その変更は失われます。」と出して**上書き／取りやめの2択**を聞く |
| 22 | 名前を付けて保存の同名確認 | `dialog.showSaveDialog` が OS 標準の上書き確認を出すため、**アプリ側では重ねて聞かない**。`docs/04` 第7章の3択（上書き／別名／中止）は、出力先を自分で組み立てる Phase 2 の結合・分割の話である |

### D. 保存の入口と保存後の状態

| # | 論点 | 決定 |
|---|------|------|
| 23 | 入口 | ツールバーの保存ボタン、「ファイル」メニューの「保存」「名前を付けて保存…」、Ctrl+S／Ctrl+Shift+S（`docs/04` 第8章） |
| 24 | 保存ボタンの状態 | 文書が無いとき・保存中は無効。**dirty でなくても押せる**（回転していない文書をそのまま別名で出したいことがある）。ただし上書き保存は dirty でないとき何もしない |
| 25 | 名前を付けて保存の初期名 | 元の名前のまま同じフォルダーを既定にする。OS の上書き確認に委ねる（確定事項22） |
| 26 | 保存後のタブ | **名前を付けて保存したら、タブは新しいファイルへ移る**（以後の上書き保存は新しいほうへ行く）。名前・パス・最近使ったファイルも差し替える |
| 27 | 保存後の dirty | 落とす。plan の**基準**を保存した並びへ置き直す（`plan` を `[{src:0,rotate:0}...]` に振り直す）。振り直さないと、保存直後なのに dirty のままになる |
| 28 | 保存後の undo 履歴 | **捨てない。**保存後も Ctrl+Z で戻せる。ただし戻すと再び dirty になる |
| 29 | 保存後の画面 | 開き直さない。並びは既に画面へ映っている（`spec-1-5` 確定事項43）ので、再読み込みは体感を落とすだけである |
| 30 | 成功の伝え方 | 帯を「保存しました」に変えて数秒で消す。ダイアログを出さない |

### E. 未保存の3択（F-06-5 の残り）

| # | 論点 | 決定 |
|---|------|------|
| 31 | 3択へ差し替える | `renderer/confirm-discard.js` の2択（閉じる／キャンセル）を、**「保存」「保存しない」「キャンセル」**の3択にする（`docs/04` 第7章） |
| 32 | 既定のフォーカス | 安全側の**キャンセル**に置く（`docs/04` 第7章）。Esc も同じ扱い |
| 33 | ボタンの色 | 「保存しない」を `--danger` で塗る。破棄にあたるのはこちらである |
| 34 | 「保存」を選んだとき | 保存を待ってから閉じる。**保存に失敗したら閉じない**（編集を消さない） |
| 35 | 保存先が無いとき | 塊⑤ の時点では、開いた文書には必ずパスがある。将来「新規作成」が入ったら名前を付けて保存へ回す |
| 36 | 終了時の複数タブ | 既存の `askAll()` の作法を保つ。1枚ずつ聞き、**1つでも取りやめたら終了そのものを中止する**（`spec-1-5` 確定事項56） |

### F. 画面と鍵

| # | 論点 | 決定 |
|---|------|------|
| 37 | 保存ボタンの位置 | ツールバー左端の「開く」の隣。アイコンは既存の `save`。名前を付けて保存は**メニューとキーだけ**にし、ツールバーには出さない（幅を食うため） |
| 38 | `saveAs` アイコン | **足さない。**ツールバーに出さないなら要らない。`docs/04` 第9章の一覧からは落とす |
| 39 | Ctrl+S の届き方 | メニューの accelerator で受ける。`viewer-controls.js` の `handleKey` は `ctrlKey` で早期 return するため、レンダラー側の `keydown` には頼らない |
| 40 | 保存中の見た目 | 保存ボタンを無効にし、帯に「保存しています」と中止ボタンを出す。画面全体は塞がない |

---

## ⑤-b の範囲（宣言のみ。着手時に詰める）

| 項目 | いま分かっていること |
|---|---|
| 抽出（F-02-4） | `copyPages` で3枚 1.3ms。しおり・AcroForm は**必ず消える**。出力先は `showSaveDialog` |
| 挿入（F-02-5） | `file-io.js` の `readPdf` は `.pdf` 以外を弾くので画像用の口が要る。pdf-lib が埋め込めるのは PNG と JPEG だけ。`copyPages` した内部リンクは壊れる |
| パスワード付き PDF（F-06-6） | `onPassword(updatePassword, code)` で聞く。fixture は依存なしで作れる（実測D）。**開けても保存はできない**（確定事項12） |
| 引数と単一インスタンス（F-07-1） | `docs/03` 第3章のとおり。`PENDING_WINDOW_MS` の実測は Phase 5 のまま |

---

## 足りない部品

### 新しいモジュール

| ファイル | 役割 |
|---|---|
| `task-runner.js` | メイン。`utilityProcess` の起動・進捗の中継・中止・一時ファイルの後始末 |
| `worker/pdf-task.js` | ワーカーのエントリ。`{source, pages, ops, target}` を受けて5段を回す |
| `worker/op-pages.js` | `pages` を pdf-lib の文書へ当てる純関数。Electron に依存しない |
| `renderer/save.js` | 保存の指揮。plan を取り、帯を出し、結果をタブへ返す |

### 既存への追記

`file-io.js`（`pickSavePath`・`.bak`・照合）／`preload.js`（`taskAPI`）／
`main.js`（`task:*` の IPC・メニューの保存項目）／`index.html`（保存ボタン）／
`renderer/confirm-discard.js`（3択）／`renderer/tabs.js`（保存後のパスの差し替え）／
`package.json`（`build.files` に `task-runner.js` と `worker/**`）

### fixture

`test/fixtures/build.js` に `encrypted.pdf`（RC4 40bit・ユーザー `user1`／オーナー `owner1`）と
`broken.pdf`（途中で切った PDF）を足す。**依存は増えない**（実測D）。

---

## テストの範囲

| 層 | 対象 |
|---|---|
| 依存なし | `worker/op-pages.js` の `pages` 適用、`.bak` の名前の組み立て、一時ファイル名、照合の判定、進捗の段の並び |
| 依存あり | `worker/pdf-task.js` を fixture に当てた検証（並び・回転・ページ数）、`file-io.js` の書き込み・EPERM の翻訳 |
| 画面 | 保存ボタンの有効・無効、帯の文言、3択の分岐、保存後の dirty とタブのパス |
| 起動確認 | `SIGK_SMOKE_SAVE=<出力先>` を足し、開く → 回す → 保存 → 出力を pdf.js で読み直すまでを通す |

---

## 完了の判定

1. 編集した文書を Ctrl+S で上書き保存でき、開き直すと編集が残っている
2. 名前を付けて保存でき、タブが新しいファイルへ移る
3. 上書き保存で `.bak` が出来ている
4. 1,000ページの保存で帯に進捗が出て、中止ボタンで止まり、**元ファイルが無傷**で一時ファイルも残らない
5. 未保存のタブを閉じると3択が出て、「保存」で保存されてから閉じる
6. 壊れた PDF で「保存できません」と出て、成功したように見えない
7. 元ファイルを外部で書き換えてから保存すると確認が出る
8. `npm test` が緑で、`npm run dist` の配布物でも `SIGK_SMOKE=1` の起動と保存が通る

---

## ユーザーの確定（2026-09-01）

起草時に残した論点7件は、同日ユーザーが決定した。**5件は起草時の推し案どおり、2件は
推し案と異なる**。異なった2件は、その理由と引き受ける代償をここに残す。

| # | 論点 | 決定 | 推し案との異同 |
|---|------|------|---|
| 1 | 塊⑤ の分割 | **割らない。1本の PR で仕上げる** | **異なる**（推しは ⑤-a／⑤-b の2本） |
| 2 | `rename` が EPERM のとき | **断る。**直接上書きへ切り替えない | 推しどおり |
| 3 | 暗号化 fixture | **自前で生成する。**qpdf は足さない | 推しどおり |
| 4 | 削除してもサイズが減らない | **既知の限界として書き残す** | 推しどおり |
| 5 | `PageLabels` | **並びに合わせて作り直す** | **異なる**（推しは既知の限界） |
| 6 | 保存後の undo 履歴 | **捨てない** | 推しどおり |
| 7 | 挿入で受ける形式 | **PDF ＋ PNG/JPEG**（F-02-5 のとおり） | 推しどおり |

### 決定1: 割らない

引き受ける代償は、PR が大きくなることと、⑤-b へ送るつもりだった論点（抽出・挿入の対応形式、
パスワードの聞き方、引数の解釈）をいま確定させる必要があることの2つである。
得られるのは、Phase 1 の完了（＝常用開始。`docs/07` 決定5）が一度に来ることである。

### 決定5: `PageLabels` を作り直す

起草時は「本アプリの画面は 1..N の連番なので、ずれても画面上は分からない」ことを理由に
既知の限界を推した。ユーザーの判断は**保存したファイルの正しさを優先する**というもので、
「他のビューアで開いたときだけ壊れて見える」状態を残さない。

引き受ける代償は実装量である。`/PageLabels` は「このページからこの様式」という**範囲の列**で
持つため、任意の並べ替えのあとは範囲がまとまらず、**1ページ1エントリへ展開する**ことに
なると見込まれる。ファイルサイズと組み立て時間への影響、および `/PageLabels` を持たない
文書の扱いは実測して確定事項へ書く。

---

## 未確定のまま残すもの

| 項目 | 扱い |
|---|---|
| メモリの合算（pdf.js ＋ ワーカー） | ワーカー単体で rss 82MB（1,000ページ）と分かったが、レンダラーとの合算は未取得。**Phase 1 末の非機能要件の実測**で測る |
| `PENDING_WINDOW_MS` の実測 | Phase 5 のまま（`docs/07` 第5章） |
| 出力先の同名確認の3択 | Phase 2（結合・分割）で要る。⑤-a は OS のダイアログに委ねる（確定事項22） |
| 見開き表示（F-01-1 後半） | Phase 1 完了時に決める（`docs/07` 積み残し） |
